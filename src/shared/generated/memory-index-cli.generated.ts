// GENERATED FILE — DO NOT EDIT BY HAND.
//
// Produced by scripts/generate-memory-index-cli.mjs (esbuild bundle of
// scripts/memory-index-cli-entry.ts + src/shared/memory-index-core.ts).
// Regenerate with `node scripts/generate-memory-index-cli.mjs` (runs inside
// `npm run build:main`). The drift test guards byte-identity.

export const MEMORY_INDEX_MJS: string = "// scripts/memory-index-cli-entry.ts\nimport * as fs2 from \"fs\";\nimport * as path2 from \"path\";\n\n// src/shared/memory-index-core.ts\nvar MEMORY_INDEX_BUDGET_BYTES = 25e3;\nvar MEMORY_INDEX_BUDGET_LINES = 200;\nvar STALE_ACTIVE_DAYS = 14;\nvar CAP_PRESSURE_RATIO = 0.8;\nvar DISCLOSURE_FORMAT_MARKER = \"<!-- disclosure-format: v2 -->\";\nvar MEMORY_ID_GRAMMAR = /^mb-\\d{4}-\\d{2}-\\d{2}-[a-z0-9-]+$/;\nvar MEMORY_DETAILS_DIR = \".lares/supervisor/memory/details/\";\nvar MEMORY_STATUSES = [\"active\", \"done\", \"note\", \"archived\"];\nvar _enc = new TextEncoder();\nfunction utf8ByteLength(str) {\n  return _enc.encode(str).length;\n}\nfunction countLines(text) {\n  const n = text.replace(/\\r\\n/g, \"\\n\");\n  let count = 1;\n  for (let i = 0; i < n.length; i++) if (n.charCodeAt(i) === 10) count++;\n  return count;\n}\nfunction isValidMemoryId(id) {\n  return MEMORY_ID_GRAMMAR.test(id);\n}\nvar _SCOPED_PKG_RE = /@[a-z0-9][a-z0-9-]*\\/[a-z0-9][a-z0-9._-]*/g;\nfunction detectBareScopedPkg(text) {\n  const noFenced = text.replace(/```[\\s\\S]*?```/g, \" \");\n  const noInline = noFenced.replace(/`[^`\\n]*`/g, \" \");\n  const out = [];\n  let m;\n  _SCOPED_PKG_RE.lastIndex = 0;\n  while ((m = _SCOPED_PKG_RE.exec(noInline)) !== null) out.push(m[0]);\n  return out;\n}\nvar _CAPSULE_HEADING = /^##\\s+(mb-\\S+?):\\s*(.*)$/;\nvar _FIELD_LINE = /^-\\s+([a-z][a-z-]*):\\s*(.*)$/;\nfunction parseIndex(text) {\n  const raw = text;\n  const normalized = raw.replace(/\\r\\n/g, \"\\n\");\n  const lines = normalized.split(\"\\n\");\n  let hasMarker = false;\n  let markerMismatch = false;\n  for (const line of lines) {\n    const t = line.trim();\n    if (t === DISCLOSURE_FORMAT_MARKER) {\n      hasMarker = true;\n      break;\n    }\n    if (/^<!--\\s*disclosure-format:/.test(t)) markerMismatch = true;\n  }\n  const headingOffsets = [];\n  {\n    let offset = 0;\n    for (let i = 0; i < lines.length; i++) {\n      if (_CAPSULE_HEADING.test(lines[i])) headingOffsets.push({ offset, lineIdx: i });\n      offset += lines[i].length + 1;\n    }\n  }\n  const firstCapsuleOffset = headingOffsets.length ? headingOffsets[0].offset : normalized.length;\n  const preamble = normalized.slice(0, firstCapsuleOffset);\n  const handoffReadFirst = parseHandoff(preamble);\n  const entries = [];\n  for (let h = 0; h < headingOffsets.length; h++) {\n    const blockStart = headingOffsets[h].offset;\n    const blockEnd = h + 1 < headingOffsets.length ? headingOffsets[h + 1].offset : normalized.length;\n    const block = normalized.slice(blockStart, blockEnd);\n    const blockLines = block.split(\"\\n\");\n    const hm = blockLines[0].match(_CAPSULE_HEADING);\n    const id = hm[1];\n    const title = hm[2].trim();\n    const fields = {};\n    for (let i = 1; i < blockLines.length; i++) {\n      const fm = blockLines[i].match(_FIELD_LINE);\n      if (fm) {\n        const key = fm[1];\n        if (!(key in fields)) fields[key] = fm[2].trim();\n      }\n    }\n    entries.push({\n      id,\n      title,\n      status: fields[\"status\"] ?? \"\",\n      date: fields[\"date\"] ?? null,\n      owner: fields[\"owner\"] ?? null,\n      consequence: fields[\"consequence\"] ?? null,\n      state: fields[\"state\"] ?? null,\n      openLoop: fields[\"open-loop\"] ?? null,\n      expires: fields[\"expires\"] ?? null,\n      expiresWhen: fields[\"expires-when\"] ?? null,\n      readIf: fields[\"read-if\"] ?? null,\n      detail: fields[\"detail\"] ?? null,\n      fields,\n      blockStart,\n      blockEnd,\n      idMalformed: !isValidMemoryId(id)\n    });\n  }\n  return {\n    raw,\n    normalized,\n    byteLength: utf8ByteLength(raw),\n    lineCount: countLines(raw),\n    hasMarker,\n    markerMismatch,\n    preamble,\n    handoffReadFirst,\n    entries,\n    bareScopedPkgs: detectBareScopedPkg(normalized).map((token) => ({ token }))\n  };\n}\nfunction parseHandoff(preamble) {\n  const lines = preamble.split(\"\\n\");\n  let i = 0;\n  for (; i < lines.length; i++) {\n    if (/^##\\s+handoff-read-first\\s*$/.test(lines[i].trim() ? lines[i] : \"\")) break;\n  }\n  if (i >= lines.length) return null;\n  const out = [];\n  for (let j = i + 1; j < lines.length; j++) {\n    const line = lines[j];\n    if (/^##\\s+/.test(line)) break;\n    const m = line.match(/^\\s*\\d+\\.\\s+(\\S+)/);\n    if (m) out.push(m[1].trim());\n  }\n  return out;\n}\nfunction hard(cls, id, message) {\n  return { cls, severity: \"hard\", id, message };\n}\nfunction advisory(cls, id, message) {\n  return { cls, severity: \"advisory\", id, message };\n}\nfunction validateParsed(parsed) {\n  const hardFindings = [];\n  const advisoryFindings = [];\n  if (!parsed.hasMarker) {\n    hardFindings.push(\n      hard(\n        \"legacy-format\",\n        null,\n        parsed.markerMismatch ? `disclosure-format marker mismatched; expected ${DISCLOSURE_FORMAT_MARKER}` : `missing ${DISCLOSURE_FORMAT_MARKER}`\n      )\n    );\n  }\n  const seen = /* @__PURE__ */ new Set();\n  for (const e of parsed.entries) {\n    if (e.idMalformed) {\n      hardFindings.push(hard(\"malformed-schema\", e.id, `malformed memory id: ${e.id}`));\n    } else if (seen.has(e.id)) {\n      hardFindings.push(hard(\"duplicate-id\", e.id, `duplicate memory id: ${e.id}`));\n    }\n    seen.add(e.id);\n    if (!MEMORY_STATUSES.includes(e.status)) {\n      hardFindings.push(hard(\"malformed-schema\", e.id, `invalid status \"${e.status}\" for ${e.id}`));\n    }\n    for (const [key, val] of [\n      [\"consequence\", e.consequence],\n      [\"state\", e.state],\n      [\"read-if\", e.readIf]\n    ]) {\n      if (!val) hardFindings.push(hard(\"missing-field\", e.id, `missing required field \"${key}\" on ${e.id}`));\n    }\n    if (e.status === \"active\" && !e.expires && !e.expiresWhen && !e.openLoop) {\n      hardFindings.push(\n        hard(\"missing-field\", e.id, `active entry ${e.id} names no exit (expires / expires-when / open-loop)`)\n      );\n    }\n  }\n  if (parsed.handoffReadFirst) {\n    const idSet = new Set(parsed.entries.map((e) => e.id));\n    if (parsed.handoffReadFirst.length > 5) {\n      hardFindings.push(hard(\"invalid-handoff\", null, `handoff-read-first exceeds 5 entries`));\n    }\n    for (const hid of parsed.handoffReadFirst) {\n      if (!isValidMemoryId(hid) || !idSet.has(hid)) {\n        hardFindings.push(hard(\"invalid-handoff\", hid, `handoff-read-first references unknown/invalid id: ${hid}`));\n      }\n    }\n  }\n  for (const { token } of parsed.bareScopedPkgs) {\n    hardFindings.push(hard(\"bare-scoped-pkg\", null, `unbackticked package token in prose: ${token}`));\n  }\n  if (parsed.byteLength > MEMORY_INDEX_BUDGET_BYTES) {\n    hardFindings.push(\n      hard(\"byte-budget\", null, `index is ${parsed.byteLength} bytes (budget ${MEMORY_INDEX_BUDGET_BYTES})`)\n    );\n  }\n  if (parsed.lineCount > MEMORY_INDEX_BUDGET_LINES) {\n    hardFindings.push(\n      hard(\"line-budget\", null, `index is ${parsed.lineCount} lines (budget ${MEMORY_INDEX_BUDGET_LINES})`)\n    );\n  }\n  if (parsed.byteLength / MEMORY_INDEX_BUDGET_BYTES > CAP_PRESSURE_RATIO || parsed.lineCount / MEMORY_INDEX_BUDGET_LINES > CAP_PRESSURE_RATIO) {\n    advisoryFindings.push(\n      advisory(\"cap-pressure\", null, `index over ${Math.round(CAP_PRESSURE_RATIO * 100)}% of a budget`)\n    );\n  }\n  return { hard: hardFindings, advisory: advisoryFindings };\n}\nfunction epochDay(dateStr) {\n  const ms = Date.parse(dateStr + \"T00:00:00Z\");\n  return Number.isNaN(ms) ? NaN : Math.floor(ms / 864e5);\n}\nfunction projectParsed(parsed, opts) {\n  const { hard: hardFindings } = validateParsed(parsed);\n  const today = opts.nowISO.slice(0, 10);\n  const todayDay = epochDay(today);\n  const dropped = [];\n  const conditionReview = [];\n  const staleActive = [];\n  const dropRanges = [];\n  for (const e of parsed.entries) {\n    if (e.status !== \"active\") continue;\n    if (e.expires) {\n      const ed = epochDay(e.expires);\n      if (!Number.isNaN(ed) && !Number.isNaN(todayDay) && ed < todayDay) {\n        dropped.push(e.id);\n        dropRanges.push([e.blockStart, e.blockEnd]);\n        continue;\n      }\n    }\n    if (e.expiresWhen) conditionReview.push(e.id);\n    if (e.date) {\n      const dd = epochDay(e.date);\n      if (!Number.isNaN(dd) && !Number.isNaN(todayDay) && todayDay - dd > STALE_ACTIVE_DAYS) {\n        staleActive.push(e.id);\n      }\n    }\n  }\n  let injectText = \"\";\n  if (hardFindings.length === 0) {\n    if (dropRanges.length === 0) {\n      injectText = parsed.normalized;\n    } else {\n      dropRanges.sort((a, b) => a[0] - b[0]);\n      let cursor = 0;\n      const parts = [];\n      for (const [start, end] of dropRanges) {\n        parts.push(parsed.normalized.slice(cursor, start));\n        cursor = end;\n      }\n      parts.push(parsed.normalized.slice(cursor));\n      injectText = parts.join(\"\");\n    }\n  }\n  const budget = {\n    bytes: parsed.byteLength,\n    lines: parsed.lineCount,\n    byteBudget: MEMORY_INDEX_BUDGET_BYTES,\n    lineBudget: MEMORY_INDEX_BUDGET_LINES,\n    byteRatio: parsed.byteLength / MEMORY_INDEX_BUDGET_BYTES,\n    lineRatio: parsed.lineCount / MEMORY_INDEX_BUDGET_LINES,\n    overBudget: parsed.byteLength > MEMORY_INDEX_BUDGET_BYTES || parsed.lineCount > MEMORY_INDEX_BUDGET_LINES\n  };\n  return { injectText, dropped, conditionReview, staleActive, budget, hard: hardFindings };\n}\n\n// src/main/memory-index/io.ts\nimport * as fs from \"fs\";\nimport * as path from \"path\";\nfunction hard2(cls, id, message) {\n  return { cls, severity: \"hard\", id, message };\n}\nfunction isInsideDir(target, dir) {\n  const rel = path.relative(dir, target);\n  return rel !== \"\" && !rel.startsWith(\"..\") && !path.isAbsolute(rel);\n}\nfunction memoryDirs(workspaceRoot) {\n  const detailsDir = path.join(workspaceRoot, ...MEMORY_DETAILS_DIR.split(\"/\").filter(Boolean));\n  const supervisorDir = path.resolve(detailsDir, \"..\", \"..\");\n  const memoryMd = path.join(supervisorDir, \"memory\", \"MEMORY.md\");\n  return { supervisorDir, detailsDir, memoryMd };\n}\nfunction tryRealpath(p) {\n  try {\n    return fs.realpathSync.native(p);\n  } catch {\n    return null;\n  }\n}\nfunction validateIO(parsed, workspaceRoot) {\n  const hardFindings = [];\n  const { supervisorDir, detailsDir } = memoryDirs(workspaceRoot);\n  const canonicalDetailsDir = tryRealpath(detailsDir);\n  const referenced = /* @__PURE__ */ new Set();\n  for (const e of parsed.entries) {\n    const pointer = e.detail;\n    if (!pointer) continue;\n    const candidate = path.resolve(supervisorDir, pointer);\n    const real = tryRealpath(candidate);\n    if (real === null) {\n      hardFindings.push(hard2(\"detail-missing\", e.id, `detail file for ${e.id} does not exist: ${pointer}`));\n      continue;\n    }\n    if (canonicalDetailsDir === null || !isInsideDir(real, canonicalDetailsDir)) {\n      hardFindings.push(\n        hard2(\"detail-escape\", e.id, `detail pointer for ${e.id} resolves outside ${MEMORY_DETAILS_DIR}: ${pointer}`)\n      );\n      continue;\n    }\n    referenced.add(real);\n  }\n  if (canonicalDetailsDir !== null) {\n    let dirents = [];\n    try {\n      dirents = fs.readdirSync(detailsDir, { withFileTypes: true });\n    } catch {\n      dirents = [];\n    }\n    for (const d of dirents) {\n      if (!d.isFile() && !d.isSymbolicLink()) continue;\n      const real = tryRealpath(path.join(detailsDir, d.name));\n      if (real === null) continue;\n      if (!referenced.has(real)) {\n        hardFindings.push(hard2(\"orphan-details\", null, `orphan detail file with no index entry: ${d.name}`));\n      }\n    }\n  }\n  return { hard: hardFindings, advisory: [] };\n}\nfunction deriveWorkspaceRootFromIndexPath(indexPath) {\n  const dir = path.dirname(path.resolve(indexPath));\n  const suffix = path.join(\".lares\", \"supervisor\", \"memory\");\n  const tail = path.sep + suffix;\n  if (dir.toLowerCase().endsWith(tail.toLowerCase())) {\n    return dir.slice(0, dir.length - tail.length);\n  }\n  return null;\n}\n\n// scripts/memory-index-cli-entry.ts\nvar EXIT_OK = 0;\nvar EXIT_HARD = 1;\nvar EXIT_RUNTIME = 2;\nfunction parseArgs(argv) {\n  const command = argv[0] ?? \"\";\n  let file = null;\n  let nowISO = null;\n  for (let i = 1; i < argv.length; i++) {\n    const a = argv[i];\n    if (a === \"--now\") {\n      nowISO = argv[++i] ?? null;\n    } else if (a.startsWith(\"--now=\")) {\n      nowISO = a.slice(\"--now=\".length);\n    } else if (!a.startsWith(\"-\")) {\n      file = a;\n    }\n  }\n  return { command, file, nowISO };\n}\nfunction readIndexFile(file) {\n  const resolved = path2.resolve(file);\n  const real = fs2.realpathSync(resolved);\n  return fs2.readFileSync(real, \"utf8\");\n}\nfunction printFindings(findings) {\n  for (const f of findings) {\n    const where = f.id ? ` [${f.id}]` : \"\";\n    process.stderr.write(`${f.severity.toUpperCase()} ${f.cls}${where}: ${f.message}\n`);\n  }\n}\nfunction main() {\n  const { command, file, nowISO } = parseArgs(process.argv.slice(2));\n  if (command !== \"validate\" && command !== \"project\") {\n    process.stderr.write(`usage: memory-index.mjs <validate|project> [--now <ISO>] <index-file>\n`);\n    return EXIT_RUNTIME;\n  }\n  if (!file) {\n    process.stderr.write(`error: no index file given\n`);\n    return EXIT_RUNTIME;\n  }\n  let text;\n  try {\n    text = readIndexFile(file);\n  } catch (err) {\n    process.stderr.write(`RUNTIME read error: ${err.message}\n`);\n    return EXIT_RUNTIME;\n  }\n  let parsed;\n  try {\n    parsed = parseIndex(text);\n  } catch (err) {\n    process.stderr.write(`RUNTIME parse error: ${err.message}\n`);\n    return EXIT_RUNTIME;\n  }\n  const pure = validateParsed(parsed);\n  const workspaceRoot = deriveWorkspaceRootFromIndexPath(file);\n  const ioHard = workspaceRoot ? validateIO(parsed, workspaceRoot).hard : [];\n  const hard3 = [...pure.hard, ...ioHard];\n  const advisory2 = pure.advisory;\n  if (command === \"validate\") {\n    process.stdout.write(JSON.stringify({ hard: hard3, advisory: advisory2 }, null, 2) + \"\\n\");\n    printFindings([...hard3, ...advisory2]);\n    return hard3.length > 0 ? EXIT_HARD : EXIT_OK;\n  }\n  const now = nowISO ?? (/* @__PURE__ */ new Date()).toISOString();\n  const projection = projectParsed(parsed, { nowISO: now });\n  if (projection.hard.length > 0) {\n    printFindings(projection.hard);\n    process.stderr.write(`refusing to project a HARD-invalid index\n`);\n    return EXIT_HARD;\n  }\n  process.stdout.write(projection.injectText);\n  process.stderr.write(\n    JSON.stringify({\n      dropped: projection.dropped,\n      conditionReview: projection.conditionReview,\n      staleActive: projection.staleActive,\n      budget: projection.budget,\n      advisory: advisory2\n    }) + \"\\n\"\n  );\n  return EXIT_OK;\n}\nprocess.exit(main());\n";
