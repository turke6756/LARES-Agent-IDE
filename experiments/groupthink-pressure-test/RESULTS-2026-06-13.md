# Groupthink pressure test results — 2026-06-13

Read-only test run. No source/test/config modified. Covers
`groupthink-pressure.test.js` under both the Windows `node` runtime and the
WSL `node` runtime.

## Environment

| Runtime | node version |
|---------|--------------|
| Windows | `v24.13.0`   |
| WSL     | `v18.19.1`   |

## Build

- Command: `npm run build:main` (run from `C:\Users\turke\Projects\AgentDashboard`)
- Result: **SUCCESS** — clean `tsc -p tsconfig.main.json`, no diagnostics, no output.
- Target compiled file present: `dist/main/main/orchestration/groupthink-pressure.test.js` (Jun 13 08:10).

This suite mixes **INVARIANT-*** (must hold; a FAIL = regression) and
**PROBE-*** (pins a known weakness; PASS = weakness still present, FAIL =
weakness possibly fixed → promote candidate).

---

## groupthink-pressure.test.js

### Windows (node v24.13.0) — **7 passed, 0 failed**

Verbatim final tally:
```
7 passed, 0 failed
```
```
ok  PROBE-T1 serial: a reviewer turn with a ts identical to its previous turn is never relayed (timeout stall)
ok  PROBE-T2 serial: a stale pre-existing plan file ends the run after lead turn-1 with zero deliberation
ok  PROBE-T3 serial: lead stuck status=working with no output outlives 12× the stall timeout; only abort ends it
ok  PROBE-T4 parallel: R3 turnComplete before the file write throws no_plan_written while the plan lands moments later
ok  INVARIANT-T5 serial: lead crashing before it can accept the relay throws exited-with-status
ok  INVARIANT-T6 serial: plan written on lead turn-1 ends the run with no reviewer launch
ok  INVARIANT-T7 serial: aborting mid-wait rejects with AbortError well before any timeout
```

### WSL (node v18.19.1) — **7 passed, 0 failed**

Verbatim final tally:
```
7 passed, 0 failed
```
Identical case list and outcomes to the Windows run (all 7 `ok`). Exit code 0.
Unlike the two supervisor suites, this file's require chain does **not** pull
in `uuid`/`database.js`/`supervisor/index.js`, so it runs cleanly on WSL
node 18 with no `ERR_REQUIRE_ESM`.

---

## Per-kind breakdown (both runtimes identical)

- INVARIANT failures (regressions): **none** (T5, T6, T7 all pass).
- PROBE failures (possible fixes / promote candidates): **none** (T1–T4 all
  pass — all four known weaknesses still present; nothing fixed).

## Windows vs. WSL divergence

**None.** `groupthink-pressure.test.js` produces `7 passed, 0 failed` on both
runtimes with the same case list. (Contrast: the two handshake suites — see
`experiments/handshake-pressure-test/RESULTS-2026-06-13.md` — fail to *run* on
WSL node 18 due to `ERR_REQUIRE_ESM` from the ESM-only `uuid` package. This
groupthink suite is unaffected because it doesn't import that chain.)

## Summary

| File | Windows | WSL |
|------|---------|-----|
| groupthink-pressure.test.js | 7 passed, 0 failed | 7 passed, 0 failed |

No regressions, no fixed probes, no platform divergence for this suite.
