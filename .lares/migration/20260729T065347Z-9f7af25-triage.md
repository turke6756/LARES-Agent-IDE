# Memory & Lessons v2 — Migration Triage Table (IMMUTABLE)

- **snapshot-id:** `20260729T065347Z-9f7af25`
- **archive (permanent, never deleted):** `.lares/archive/20260729T065347Z-9f7af25/`
- **produced-by:** WP-I1 worker, 2026-07-28
- **design ground truth:** `plans/memory-lessons-v2-proposal.md` §7 (behavioral triage), §8 (migration), §4 capsule schema + `mb-YYYY-MM-DD-slug` id scheme
- **table_hash (sha256):** `42289347284b7a5d752ed40df08a6cd21f20bd4751ff0340c0727b33129ad05c`
- **table_hash recipe (WP-I2 recomputes EXACTLY this):**
  `awk 'seen && /^<!-- TABLE-HASH-CONTENT-END -->/{exit} seen{print} /^<!-- TABLE-HASH-CONTENT-START -->/{seen=1}' <thisfile> | sha256sum`
  — i.e. sha256 over the bytes of every line strictly BETWEEN the two
  `TABLE-HASH-CONTENT` marker lines (markers themselves excluded, LF line endings).
  The `table_hash` field above sits OUTSIDE that region, so recording it does not
  alter the hashed content. Edward's `memory:migrationApprove` sign-off (WP-H3)
  binds a `migration_approvals` row to (`snapshot_id`, `table_hash`); WP-I2 refuses
  to migrate unless the recomputed hash matches an approval row.

> This artifact is **immutable** once its hash is recorded. Do not edit the region
> between the markers after sign-off — any edit invalidates the approval binding.
> The archive is preserved permanently; a crash before approval leaves an
> unreferenced archive dir that is retained on disk, never deleted.

---

<!-- TABLE-HASH-CONTENT-START -->

## Archived source inventory (byte-exact, structure preserved)

Full existing memory tree + both worker behavioral files were copied to
`.lares/archive/20260729T065347Z-9f7af25/`; originals are byte-unchanged. sha256:

| Archived path (under archive dir) | sha256 |
|---|---|
| `supervisor/memory/MEMORY.md` | `dbc2e598e23ae96470040134727a0e770e30febc26c513077c5ea20b98c3d172` |
| `supervisor/memory/MEMORY.md.bak.20260525-135717-5416` | `431f600b12751fa2db1da45d1e89cc01c6673fc1e2ec7cbcbaf5629508762ea1` |
| `supervisor/memory/context-overhead-findings.md` | `95a76620d2261107d15978e69a326c49aa8b31ad7d8c512b7ec6b6625216b2b1` |
| `workers/claude/behavioral.md` | `0a4c51e66125f00d672aabec72f3583b31d536f7528f26b5ed53dcd7ae3009b3` |
| `workers/codex/behavioral.md` | `4b2b8868af753cda70abe1d1590fa52b989866c2b29bdde88a95fffc7c018071` |

There is **no `details/**` subtree** in the live memory dir at snapshot time (the
current index keeps everything inline); the archive therefore contains only the
files above. `MEMORY.md.bak.*` is preserved verbatim but is NOT triaged (a May
backup, superseded by the live `MEMORY.md`).

---

## PART A — Memory survivors (open loops → `mb-` capsules)

Per §8, "~3–5 genuine open loops" carry forward as capsules; everything else is
archived chronicle (git/DB already hold completed work — fails the §2 worthiness
test). Candidate survivors below, strongest first. `strength` flags which are the
core survivors vs. fold/note-tier candidates Edward may cut.

| proposed id | strength | one-line state | exit condition | source (MEMORY.md block) |
|---|---|---|---|---|
| `mb-2026-07-28-memory-lessons-v2-execution` | CORE | Memory & Lessons v2 build in flight — 15 one-turn WPs, strictly sequential, full autonomy granted; WP-R…H3 landed, I1 (this) → deploy gate → I2 remain. | expires-when: WP-I2 signed migration lands and the fresh index is live | `v2-EXECUTION BLOCK` |
| `mb-2026-07-28-gated-work-awaiting-deploy` | CORE | Multiple workstreams DONE+GATED but UNCOMMITTED / unpushed (~45 commits ahead of origin/master); commit+push authorization and `npm run restart` pending for git-native envelope, terminal-log retention, memory-hardening, checkpoint-surface, guard hook. | expires-when: Edward authorizes commit/push and relaunches the app to deploy pending features | `RETENTION`, `GUARD-HOOK`, git-native `REMAINING = ALL EDWARD`, commit-split blocks |
| `mb-2026-07-28-git-discard-guard-deploy` | CORE | git-discard PreToolUse guard hook DONE+GATED but uncommitted; one queued 1-line edit (add `"decision":"deny"` for future Grok coverage) not yet made; supervisor+researcher scaffolds not yet covered (workers only). | expires-when: guard committed + deployed (Edward restart) and the queued grok-deny edit landed | `GUARD-HOOK BLOCK` |
| `mb-2026-07-28-codex-worker-auth-unknown` | CORE | Open question to Edward: how do our Codex workers authenticate today? `OPENAI_API_KEY` is absent from `src/` → likely ChatGPT sign-in under `CODEX_HOME`, which would break every Codex worker if per-workspace `CODEX_HOME` (research "Path B") were adopted. | expires-when: Edward confirms the Codex auth mechanism / Path B is decided | `TWO DEFECTS IN THE RESEARCH` (defect 1) |
| `mb-2026-07-28-bak-retention-cleanup` | CORE | 20 `.bak` files / 196 KB under `.lares/`; D15 / WP-J retention policy planned, cleanup separately authorized (not yet run). | expires-when: WP-J `.bak` retention lands and the authorized cleanup executes | proposal §9 D15 / plan WP-J |
| `mb-2026-07-28-behavioral-md-context-tax` | FOLD | `.lares/workers/claude/behavioral.md` = 36,759 B ≈ 9.2k tokens, 29 entries, append-only, read by every worker at task start; Codex inherits the identical unbounded contract. Flagged to Edward, unresolved. | expires-when: v2 migration retires behavioral.md into lessons — **subsumed by `…-v2-execution`; keep only if Edward wants a standalone tracker** | `UNRESOLVED, FLAGGED TO EDWARD` |
| `mb-2026-07-28-onedrive-appasar-sweep` | NOTE | `release.stale-*` backup moved into OneDrive (~1.5 GB queued for cloud sync); a duplicate 774 MiB `app.asar` remains physically on disk (was file-locked) awaiting a sweep once the lock clears. | expires-when: duplicate `app.asar` removed and OneDrive sync location resolved | `CHECKPOINT-OUTAGE` → `OPEN FOR EDWARD` |

**Archived-only (NOT survivors — chronicle git/DB already holds; §2 worthiness fail).**
Summarized, not row-per-row: git-native envelope A0→G3.5 (all landed+gated on
master); terminal-log disk retention 9 WPs; memory-hardening WP-1a/1b/1c/2/4 +
terminal-history WP-1..3e; checkpoint-capture outage RCA + 4 checkpoint-surface WPs;
idle-agent lifecycle legs 1/2a/2b; VM clean-install defect fixes; context-analytics
WP1..WP9; commit-split landings; 2026-07-06→07-22 recovered/condensed blocks
(observability wave-2, launch P0–P5, incident remediation, P0 hardening wave,
edit-loss P4/P5, write-orchestration-script skill, false-idle fix). The
`context-overhead-findings.md` investigation (2026-07-19) is likewise archived: its
conclusions were superseded by `plans/context-overhead-review.md` and the
`supervisor-persona-capability-parity.test.ts` it recommended already landed — no
open loop remains.

---

## PART B — Behavioral triage (§7: rule / lesson / retired)

29 WB entries total (`.lares/workers/claude/behavioral.md`: WB-01…WB-28 + 1
unnumbered "When rebuilding is forbidden…"). `.lares/workers/codex/behavioral.md`
holds only WB-01 (a verbatim mirror of Claude WB-01) → same disposition. Per §7 the
behavioral.md files are **retired as files**; surviving know-how re-homes as rules
(always-read / hook) or lessons (one skill folder each, description = the trigger).

### Rules (must fire 100% — promote to always-read instruction / hook)

| WB | proposed home | one-line state | source date |
|---|---|---|---|
| WB-10 | rule (mutation-test mandate; already briefed per-WP) | A new test that passes first run is unproven — mutate the code to make it fail, restore by re-Edit (never VCS revert on a shared tree). | 2026-07-14 |
| WB-11 | rule (line-ending safety; overlaps live `WORKER_CLAUDE_MD` "never rewrite via shell pipeline") | `sed -i` on Windows rewrites the whole file to LF — never use it to revert a mutation; restore by byte-copy / re-Edit. | 2026-07-19 |
| WB-15 | rule (discard-guard family; already in git-discard hook + `WORKER_CLAUDE_MD`) | Never `git stash` to get a baseline — the tree usually carries others' uncommitted work; get the baseline non-destructively. | 2026-07-20 |
| WB-19 | rule (line-ending safety; overlaps the same CLAUDE.md rule) | Never bulk-normalize line endings across a file list — each file's original convention is the only correct target. | 2026-07-21 |

### Lessons (one skill folder each; description = the trigger condition)

| WB | proposed lesson-slug | trigger (→ description) | source date |
|---|---|---|---|
| WB-01 | `unproven-cause-say-so` | Diagnosing a bug where a symptom/code-read contradicts your clean root-cause story → separate proven-from-code from hypothesis; say "can't explain yet". | 2026-06-12 |
| WB-02 | `continuation-build-before-trusting-prompt` | Inheriting a "half-applied, non-compiling" handoff → reconcile from disk and build first; the prompt's breakage claim is a hint, not fact. | 2026-06-22 |
| WB-03 | `run-sibling-suite-not-just-named` | A change to a shared surface → run the whole sibling suite, not just the named files; a broad-scan neighbor test can silently break. | 2026-06-22 |
| WB-04 | `re-derive-inherited-values` | Handed a precomputed hash/version/count → re-derive it, and calibrate your pipeline against a known-good fixture first. | 2026-07-02 |
| WB-05 | `mirror-unmerged-contract` | Calling names a parallel worker owns that don't exist yet → mirror the contract locally via a null-tolerant accessor, don't block. | 2026-07-05 |
| WB-06 | `land-determined-seams-hand-off-coupled` | A multi-module wiring task too big for one context → land + test the fully-determined leaf pieces, hand off the coupled remainder with the exact contract. | 2026-07-06 |
| WB-07 | `acceptance-divergence-assert-truth` | A real-corpus acceptance whose spec-expected finding doesn't appear → instrument to the exact gate, assert what's actually true, report the divergence (not weaken). | 2026-07-06 |
| WB-08 | `windows-git-commit-dash-f` | Multi-line `git commit -m` in the Bash tool on Windows → use `-F <file>`/heredoc; the PowerShell `@'…'@` here-string injects literal `@`. | 2026-07-13 |
| WB-09 | `search-capability-not-named-file` | Told coverage/behavior is missing and given the file to extend → grep the capability first; it may already exist under a sibling filename. | 2026-07-14 |
| WB-12 | `absence-fixture-from-real-artifact` | Writing an absence/leak-guard test → seed the fixture from the real output artifact's vectors, then mutate; an imagined fixture passes vacuously. | 2026-07-20 |
| WB-13 | `single-call-site-is-a-sample` | A "fix the path at `<file>:<line>`" task → grep the capability for other consumers and run the real built artifact; one named site is a sample, not an inventory. | 2026-07-20 |
| WB-14 | `find-the-subprocess-shape-you-can-run` | Told the only sufficient test would kill your host → find the narrowest side-process slice of that evidence and run it; dry-run your human script too. | 2026-07-20 |
| WB-16 | `foreign-edit-is-a-fact-not-a-bug` | A shared file starts failing on symbols you never touched → confirm ownership, then leave it; "helpfully" fixing races the other worker's design. | 2026-07-20 |
| WB-17 | `windows-partial-staging-blob` | Committing one slice of a file carrying others' hunks on Windows CRLF → reconstruct `HEAD + your hunks` and stage the blob; never `git apply --cached`. | 2026-07-20 |
| WB-18 | `green-runner-is-not-green-tsc` | Added a file + tests that pass → also run `tsc --noEmit` filtered to your files; runner and typechecker resolve modules with different configs. | 2026-07-20 |
| WB-20 | `assert-private-container-not-getter` | Proving a rejected op left no state → assert the private container directly; an allocating get-or-insert getter manufactures the state you meant to disprove. | 2026-07-21 |
| WB-22 | `background-run-notification-is-truth` | An empty tail-piped file + missed process-grep → not proof a background run died; the harness completion notification is ground truth (pipes buffer to exit). | 2026-07-21 |
| WB-23 | `architect-for-committability` | A WP touching files with foreign uncommitted work → put new capability in new/clean modules, keep entangled files to thin threading, decide placement before coding. | 2026-07-21 |
| WB-24 | `fixture-where-guard-is-sole-mechanism` | Testing an override/tie-breaker rule → build the fixture so the guard alone produces the value; else the base rule keeps it green under mutation. | 2026-07-21 |
| WB-26 | `grep-emitted-strings-not-helper-name` | Gating/renaming diagnostic output → grep tests for the emitted payload strings, not just the helper symbol; a spy can assert on args without naming it. | 2026-07-22 |
| WB-27 | `trace-when-state-is-created-vs-gate` | A fix keyed to "state X exists" plus an invariant the same gate must keep → trace when X is created relative to the gate; refine to the distinguishing signal. | 2026-07-22 |
| WB-(unnumbered) | `rebuild-forbidden-esbuild-to-scratch` | Must run compiled tests but builds are forbidden (live app holds `dist/`) → esbuild-bundle the test standalone to the scratchpad + `tsc --noEmit`; never touch `dist/`. | 2026-07-22 |
| WB-28 | `inject-fake-for-subprocess-oracle` | Testing a guard that delegates an ambiguous case to a real subprocess (git/stat/DNS) → inject a fake and assert via it; a real-oracle table row stays green under a broken guard. | 2026-07-27 |

### Retired (superseded / too narrow — knowledge folds into a kept entry)

| WB | folds into | rationale | source date |
|---|---|---|---|
| WB-21 | rule WB-11 + lesson WB-17 (Windows CRLF) | Byte-offset test fails ±N with clean `git diff` → autocrlf checkout conversion. Too narrow; a corner case of the CRLF line-ending family already covered. | 2026-07-21 |
| WB-25 | lesson WB-17 (`windows-partial-staging-blob`) | Selective-staging via 3-way merge needs EOL normalization first. A niche mechanic of the same partial-staging-on-CRLF lesson; not a distinct skill. | 2026-07-22 |

**Triage totals:** 29 behavioral entries → 4 rules · 23 lessons · 2 retired.
Codex behavioral.md (WB-01 only) is covered by lesson `unproven-cause-say-so`.
Both behavioral.md files are retired as files per §7 once their survivors re-home.

<!-- TABLE-HASH-CONTENT-END -->

---

*End of immutable triage table. `table_hash` recorded in the header above.*
