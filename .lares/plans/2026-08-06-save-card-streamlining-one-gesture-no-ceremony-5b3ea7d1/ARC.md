# ARC — Save-card streamlining — one gesture, no ceremony   (plan_sku: 2026-08-06-save-card-streamlining-one-gesture-no-ceremony-5b3ea7d1 · plan_artifact_id: plan_5b3ea7d1)
<!--ARC-META {"last_refreshed_at":1786055662751,"source_cutoffs":{"folder_mtime_ms":1786055649569.5488,"ledger_updated_at":null}} -->
## Decisions
- 2026-08-06 — Hardening scope verdict: one part needs multi-agent deliberation (carry-forward equivalence predicate + dependent sweep semantics, P1/P2/P6); P3/P4/P5 need no hardening; no online research on any part. Second opinion: codex 9d6c0db0.
- 2026-08-06 — Scoping second opinion (codex `9d6c0db0`) confirmed the `candidateId` diagnosis against the current tree and corrected two proposal claims: `candidate-ack-stale` requires a stored preview draft (not universal), and auto-generated commit messages already exist (`save-card-ipc.ts:531`), making P3 a quality gap rather than a missing mechanism.
- 2026-08-06 — Deliberation `int_7c1e94af` rejected the proposal's candidate predicate and adopted **reviewed-universe equality plus proof-bearing monotonic discharge**, with a versioned `ReviewedSemanticManifest` held separate from operational candidate/token identity.
- 2026-08-06 — **Scope expansion, supervisor-flagged:** exact-object commit construction (`commit-tree` + CAS `update-ref`) becomes a **prerequisite** to enabling the sweep, not a follow-up. Carries an unresolved product decision on git hook / signing behavior — for Edward, not a worker.
- 2026-08-06 — **Baseline tag** `plan-baseline/2026-08-06-save-card-streamlining-one-gesture-no-ceremony-5b3ea7d1` created (local annotated) at `e1a6ab5a`. Local only, never pushed. Recovery framing: anything this plan deletes is one `git show <tag>:<path>` away, so deletion WPs need no copy-aside archiving. **Advisory (non-blocking):** 56 uncommitted paths existed in the worktree at tag time — the tag captures committed HEAD only, so that work is outside the recovery point.
- 2026-08-06 — **EDWARD RULING — hook and signing behaviour under exact-object commit (unblocks WP-5): OPTION 1.** Git hooks do **not** run in the Lares commit path. Evidence gathered before the ruling: the repo has **no active hooks** (`.git/hooks/` holds only untouched samples), **no `core.hooksPath`**, **no husky**, `commit.gpgsign` unset and **no signing key** — so nothing is lost today. Option 2 (non-mutating pre-`commit-tree` validation) was offered and declined as unwarranted complexity for hooks that do not exist. **Obligation on WP-5:** state this behaviour explicitly in the coordinator code and its docs, so a future hook author discovers "Lares saves do not run this" rather than silently wondering why their hook never fires. Revisit only if hooks or signing are later adopted.
## Work packages
- WP-1 Reviewed semantic manifest and normalized commit effects — **LANDED + GATED** `c26fa62b` (codex `d072e772`, reaped). Gate evidence: 3 files, purely additive (270/0, 25/0); manifest explicitly separate from operational candidate/token identity (rejected design NOT built); rename test covers source + destination + all commitPathspecs; existing JCS helper reused; 5/5 normalization tests run by supervisor; full `npm run build` clean. **Watch-out I verified:** the file adds `node:buffer` + a `src/main/**` import to a SHARED module. Every current renderer import of it is `import type`, so it erases and the bundle builds — but the first renderer *value* import will break the bundle. Keep renderer imports type-only (relevant to WP-7).
- WP-2 Fix topology digest to include the ownership input overlap actually uses — **LANDED + GATED** `386b37e7` (codex `e492772a`, reaped). Gate evidence: 2 files, own only. Load-bearing regression test present and passing — two fixtures differing ONLY in owner produce **different `topologyDigest` AND different `requiresOverlapAck`**, which is exactly the latent bug (digest equal while the acknowledgement obligation changed). Overlap status, ownership keys, structured topology and challenge atoms are now all derived from the same canonical model, so they cannot disagree. 8/8 assembler tests run by supervisor; renderer save suite 89/89. **Its `build:main` failure was verified NOT its fault:** it names `CandidateTokenSnapshot.commitEffects`, a WP-3 field — `candidate-service.ts` is dirty in-tree with zero `commitEffects` in its committed version, i.e. WP-3 mid-edit. `src/shared/` was clean, so WP-3 has not strayed into WP-1's files. Expect this to resolve when WP-3 commits; re-run `npm run build` at the WP-3 gate.
- WP-3 Bind the full commit-effect set into candidate identity — **LANDED + GATED** `edf72436` (codex `ccf0e5d8`, reaped). Gate evidence: 3 files, purely additive (+155/-0), no skips introduced. Load-bearing rename regression is exactly right — `members` stay `deepEqual` (every previously-hashed field unchanged) while `candidateId` DIFFERS, which is the latent bug: a rename could change what actually commits without changing identity. `pinnedHeadOid`/`indexFingerprint` retained as required. Supervisor ran candidate-service build+mint tests (2/2, 0 skipped) and **full `npm run build` PASSES** — which also clears WP-2's transient `CandidateTokenSnapshot.commitEffects` failure, confirming that was WP-3 mid-edit as diagnosed.
- **Residual uncertainty, WP-3:** the worker reported "3 Windows-specific path-semantics cases explicitly skipped". I could NOT reproduce any skip — git-checkpoints 36/36 with 0 skipped, candidate-service 0 skipped, renderer save 89/89. Its commit introduced no `.skip`. Unresolved but low-risk; not accepted as proven either way.
- **Trap found while gating WP-3, applies to WP-5 and anyone after:** several suites under `src/main/commit-candidates/` and `src/main/git-checkpoints/` early-return with a `console.log('  SKIP … no compatible git resolved')` and still report **PASS**. A self-skipped case proves nothing. I verified real git DOES resolve in this environment (the SKIP branch is not taken), so these tests genuinely run today — but a worker reporting "coordinator tests passed" must be checked for that line before the pass is believed.
- WP-4 Carry predicate: reviewed-universe equality plus monotonic discharge — **LANDED + GATED** `218b4bf1` (codex `bbc5a839`, reaped at 90% context). Gate evidence: exactly 7 files; `src/shared/types.ts` purely additive (+27/-0); `preview-routes.ts` is 6 lines of pure forwarding (two conditional spreads, no logic). Refuses BEFORE mint **by construction** — carry check at `candidate-service.ts:285-325`, token minted at `:357`. `ReviewedSemanticManifest` held in its own digest-keyed map, SEPARATE from `candidateId`/`tokenId`, with discharged/pending partitioning — the rejected bidirectional-equality design was NOT built. **Scope extension I granted mid-package:** the worker asked for 4 extra seams claiming it was blocked. I checked each against the tree; **two were wrong** — `buildContext` (`preview-routes.ts:391`) SPREADS the scope context and strips nothing (topology + `ownerAgentId` were already reaching it), and `mintCandidateToken(request, context)` is itself in the authorized file set and receives `currentCommitReps`, so refuse-before-mint needed no new seam. **One was right:** the carried review digest and acknowledged atoms had no route in. I authorized ONLY additive fields in `types.ts` + pure forwarding in `preview-routes.ts`. Lesson: check a worker's blocker claims against the code before granting scope.
- WP-5 Exact-object commit construction — **LANDED + GATED** `fe743334` + `5a121843` + `9080f53d` (codex `200c7530`, reaped at 100% context; docs closed by fresh codex `b2ea6e2c`). Gate evidence: `fe743334` = coordinator + 4 test suites, own files only; no `git commit --only` fallback remains; injection proofs ran against real Git and did NOT self-skip (the SKIP-pattern suites are other files); `tsc` exactly 118. `5a121843` = e2e hook expectation **rewritten, not deleted** (+8/-7) to match the bypass. The worker correctly refused to amend when a foreign commit moved HEAD between prepare and amend, isolating the fix in its own commit. **Gate FAIL I caught and made it close:** the package required the bypass be stated in code AND "in whatever doc covers the save path". Code was done; **zero docs were touched** — I verified `docs/git-native-checkpoints.md` and `docs/architecture.md` had no mention of hooks or signing. Closed by `9080f53d` (1 file, +9/-0) stating commit-tree construction, ruled choice, no hooks/no signing, the reason (a hook that rewrites the index could introduce bytes outside the reviewed tree), and the consequence for a future hook author.
- WP-6 Main-side save-sweep service with halting rules — **LANDED + GATED** `e52ad5fb` (codex `bff48b91`, reaped). Gate evidence: 6 files exactly as authorized; `types.ts` purely additive (+69/-0); `hasUnmerged` still computed repository-wide (`entries.some`, no path scoping) and commented as preceding every mint; minting is just-in-time, never pre-minted. **The halting contract is honored where it is easiest to fake:** pre-consumption (`attempt.created === false`) → `needs-attention` + continue; post-consumption (`attempt.created === true`) → **always halts**, classifying `halted-uncertain` even when a refusal object is present, preserving the refusal code for diagnosis and carrying `attemptId`/`commitOid`. That is the non-goal "never flatten a post-commit reconciliation error into a continuable package refusal", actually implemented. `markNotAttempted` stamps every remaining intent on halt, which is what makes "exactly one terminal result per input intent" true. 12/12 sweep tests, self-skip count 0, `tsc` 118.
- **WP-6b Wire the save sweep into production — INSERTED BY ME 2026-08-06, dispatched codex `6e61c2af`.** WP-6 built a correct, fully-tested sweep and **never wired it into production**. WP-7's worker found it before editing and was RIGHT; I verified: `SAVE_SWEEP_CHANNEL` exists only as a constant at `types.ts:2379` and appears nowhere else in `src/`; `SaveSweepService` is constructed nowhere outside its own module and tests; preload's `saveCard` object has no `sweep`; `WindowApi` declares none. The renderer literally could not call it. **This is a PLAN GAP, not only a worker miss** — WP-6's prose Files list contains no registration/preload seam, so a worker obeying its scope could not have wired it, and my gate passed it because I checked it against that spec. **Second occurrence of this exact failure class in this subsystem:** earlier save-card work shipped `mintCandidateToken` as dead code (`buildCandidate` always returned `token: null`) and renderer tests mocked the token in, hiding the dead bridge until a live test failed. Seam for the fix: `src/main/ipc-handlers.ts:256-264` (same `() => routes` accessor pattern as the neighbouring save-card registrations) + preload `saveCard` block at `src/preload/index.ts:176`. WP-6b's acceptance is a test that proves the REGISTERED channel reaches a real service — a directly-constructed service or a mocked `window.api.saveCard.sweep` proves nothing.
- WP-6b — **LANDED + GATED** `b4617499` (codex `6e61c2af`, reaped). Gate evidence: the production proof is real — a test stubs Electron, loads the ACTUAL `ipc-handlers` module, captures what it registers, and invokes `savecard:sweep` against a real `SaveSweepService`; delete the registration and the channel is absent from the handler map and the test fails. Preload test pins `sweep: (req) => ipcRenderer.invoke(SAVE_SWEEP_CHANNEL, req)`. **Scope overrun, accepted and recorded:** it was briefed as wiring but added ~190 lines of substantive resolution logic to `preview-routes.ts` (`resolveSweepIntent`, the `already-saved` determination, reachability via `merge-base --is-ancestor`, HEAD tree reads, frozen-manifest digests). That is WP-6 work — **WP-6 was doubly incomplete: unwired AND missing the resolver behind its own dependency interface.** Someone had to write it; better here than nowhere, but my WP-6 gate missed more than the bridge.
- **Safety question I traced at the WP-6b gate (answer: safe, fails closed):** `addSweepProofs` silently drops non-UTF8 paths from the HEAD proof map (`utf8Clean ? [...] : []`). WP-4's `currentHeadMatches` (`candidate-service.ts:1053-1057`) does `!!head && …`, so a missing entry returns `false` → the effect stays PENDING, never discharged. A non-UTF8 path can therefore never produce a false `already-saved`. **User-visible consequence for Edward, not a bug:** a package containing non-UTF8 paths can never be discharged and may sit permanently in needs-attention.
- WP-7 Renderer: server verdict replaces the local staleness comparison — **LANDED + GATED** `f6bb12a3` (codex `042fea39`, reaped at 90%). Gate evidence: 8 files as authorized; local staleness comparison GONE with no fallback left (`freshCandidateId` / `draftCandidateId !==` return zero hits repo-wide in the renderer). **Bundle hazard verified properly:** a naive grep for value imports produced 3 FALSE hits because the imports are multi-line with `import type` on an earlier line — reading the actual statements confirms all renderer imports of `shared/commit-candidates` are type-only, and the Vite build passes. Anyone re-checking this must read the statement, not grep one line. **No-retry verified against the tree, not the summary:** two `data-testid="save-card-retry"` buttons exist in `SaveCard.tsx` (~`:591`, `~:623`) — both are INVENTORY-LOAD affordances (`error` state "Nothing was written", `empty` state "Nothing to save") whose handler is `refresh`, not a re-submit, and neither is reachable from the post-submit uncertain state, which sets `submitLocked`. Renderer `tsc` 113 pre-existing, none introduced.
- WP-8 Save-all control and progress affordances — **LANDED + GATED** `885edc25` (codex `0e1175d3`, reaped). Gate evidence: 4 files as authorized; imports still type-only; no new retry affordance (the only added `retry` strings are the copy "Lares will not retry automatically"). **Auto-acknowledge non-goal verified:** `acknowledgedChallengeAtoms` are collected from each package's own `request.acknowledgedChallengeAtoms` (i.e. its review checkboxes), deduped by `atomId\0digest` — never synthesized. Tests cover BOTH directions: an unacknowledged overlap sends `acknowledgedChallengeAtoms: []` (`SaveCard.test.tsx:611`), and ticking the ack checkbox sends the atoms (`:683-690`). **`localNeedsAttention` is NOT a re-derived server verdict** — it counts packages that never became intents (no gesture handle, or `prepareForSweep()` returned null), so the server never saw them; adding it to the server's `needsAttention` is legitimate. **The "Saved 0 confirmed" line (`:949`) is reached ONLY when the sweep IPC itself threw**, so there are no server results at all; the wording "0 confirmed" plus "a commit may have been created, so Lares will not retry automatically" is the honest framing, not a false failure claim. Note: that throw path `return`s before `refreshInventory`, so the inventory can read stale after a transport-uncertain save.
- **UNPERFORMED, remains for Edward (do not let a successor mark this proven):** WP-8's spec asks for a manual multi-package save pass in the running app. No worker can do it — it needs the running Electron app and no agent can relaunch the app it runs inside. It must check: per-checkbox busy feedback during preparation/verification, Save-button and overall package progress during the sweep, the single saved/already-saved/needs-attention/halted summary, and that an uncertain halt offers no retry and makes no false failure claim.
- WP-9 Plain-language copy pass over controls and typed refusals — **LANDED + GATED** `deefad3c` (codex `fce004a5`, reaped). Gate evidence: 4 files, own only; stage-name jargon gone; overlap copy corrected from "fuses work from multiple agents or plans" to "multi-owner work" (the scoped-down truthful wording); refusal codes/taxonomy unchanged; commit-stage branch filters jargon out of server passthrough; 10 files / 89 tests run by supervisor. **Worker misattribution corrected at the gate:** it blamed 7 type errors in its files on "parallel src/shared/* contract changes" (i.e. WP-1). FALSE — repo-wide errors were 118 before AND after WP-1, WP-1's diff is purely additive (modified no existing line), and the `.token` error exists at baseline `e1a6ab5a`. They are pre-existing dirt. Do not let a later worker "fix" WP-1 for this.
- WP-10 Better default commit messages from package metadata — **LANDED + GATED** `8b2af592` (codex `4e8001cd`, reaped). Gate evidence: 3 files, own only; default now derived from package ids + turn counts with the old `Save N files` as deterministic fallback; explicit sort comparator (no locale dependence), no timestamps/randomness; control/zero-width/bidi stripped; 72-char bound by code point; message stays an optional override; provenance trailers untouched; 8/8 IPC tests run by supervisor. Two tsc errors naming save-card files were checked and are pre-existing (both test files byte-identical to baseline, not dirty).
- **Baseline dirt, established fact:** repo-wide `npx tsc --noEmit -p tsconfig.json` = **118 pre-existing errors** at this plan's baseline, unrelated to this plan. Measured before and after WP-1. Workers must add none; they must not attribute these to plan packages.
- Source: [supplements/2026-08-06-work-packages.md](supplements/2026-08-06-work-packages.md) (`PLAN-WORK-PACKAGES:v1`, validated). Human register: [OVERVIEW.md](OVERVIEW.md) (validated).
## Deliberations
- `int_7c1e94af` — part `carry-forward-equivalence-and-sweep-semantics` · rung **folded-in** · output [deliberations/2026-08-06-carry-forward-equivalence.md](deliberations/2026-08-06-carry-forward-equivalence.md) · GroupThink parallel, self-declared `orchestration_id: a1bacc4a` (cross-check only; `ran` is server-witnessed and **unavailable pre-ledger**) · folded via PLAN-INTEGRATION in `plan.md` under "## Hardening outcome". Lead/synthesizer codex, peer claude.
- `int_7c1e94af` — second output [deliberations/int_7c1e94af-claude-draft.md](deliberations/int_7c1e94af-claude-draft.md) · claude planner round-1 draft · disposition **superseded** by the synthesis, so it does not keep the intent open. Its self-declared `orchestration_id` (`a0173b0f-…`) differs from the run id — self-declaration is a cross-check only, never authority.
- Intent `int_7c1e94af` is **fully folded in**: every present `active` output is referenced from `plan.md`.
## Who did what
- `229530a1` "new propsoal" (supervisor, claude) — authored the proposal, ran scope/promote/deliberate/integrate/package.
- `9d6c0db0` (worker, codex) — independent scoping second opinion; read-only.
- GroupThink run `a1bacc4a` (codex synthesizer + claude peer) — the deliberation output above.
