# Deliberation brief — int_7c3e9a12: human-readable overview layer (A1 + B4)

You are deliberating ONE marked intent of plan `plan_e0001372` ("Bridge the
proposal-to-plan skill and the planning surface"). Read these first:

1. The source proposal: `.lares/proposals/2026-08-05-planning-surface-skill-bridge.md`
   — especially observed failure #5 (no human-readable layer) and the
   "Dual-register, both durable" design principle.
2. The already-decided sibling deliberation (BINDING — it states the interface
   it needs from YOU, in its "Readiness and human-trigger chain" section):
   `.lares/plans/2026-08-05-bridge-the-proposal-to-plan-skill-and-the-planni-e0001372/deliberations/2026-08-05-int_4f8b2d61-wp-ingest-implementability-chain.md`
3. The code (validate, do not trust prose): the `plan_tab_overviews` table and
   its readers/writers in `src/main/database.ts`, `buildPlanDocuments` in
   `src/main/plans/plan-documents.ts`, `src/main/plans/plan-lifecycle.ts`
   (the overview gate on Mark Ready), `src/main/plans/plan-ipc.ts`, and the
   Overview tab / summary band in `src/renderer/components/plan/`.
4. The skill side: the `package` playbook and contracts under
   `.lares/supervisor/.claude/skills/proposal-to-plan/references/`, and a real
   plan folder for register comparison:
   `.lares/plans/2026-08-05-provider-inclusive-groupthink-user-shapeable-pro-pigt5a83/`.

## The question

Design the human-register layer so a workspace owner can build a mental model
of a plan from the surface alone — without asking an agent to explain it in
chat. This is ONE design spanning A1 (skill writes it) and B4 (surface reads
it), because a single disk overview collides with the existing
**per-populated-tab** `plan_tab_overviews` readiness gate.

Decide, concretely:

1. **The gate collision.** `plan_tab_overviews` requires a non-empty body for
   every tab `buildPlanDocuments` reports as populated — and int_4f8b2d61's
   ingest will newly populate the Packages tab. Choose: does one disk document
   supply only the Overview tab (leaving other tabs to another mechanism), does
   it generate per-tab summaries, or does the readiness contract itself change?
   Say exactly what int_4f8b2d61's WP-INGEST-4 can depend on.
2. **Durable home + format.** Where the human overview lives on disk (the
   proposal's candidate is an `OVERVIEW.md` beside `ARC.md`), whether it is
   sectioned per tab, and how it is parsed. Prefer a shape consistent with the
   PLAN-WORK-PACKAGES:v1 precedent set by int_4f8b2d61 if you need machine
   structure.
3. **Disk↔DB direction.** Whether the surface reads disk directly, whether the
   reconciler projects it into `plan_tab_overviews` rows (same watcher settled
   seam), and the conflict policy when a human has edited an overview in the UI
   and disk then changes. Do not create a second writer.
4. **The A1 skill step.** What the `package` playbook mandates the responsible
   supervisor write, and the register rule verbatim: *written for the workspace
   owner — no sentinel names, no rung jargon, no file:line.* Include a short
   worked example of good vs. bad register. Note that scaffold content changes
   require a version bump (see the `scaffold-content-needs-version-bump`
   skill).
5. **Empty-state honesty.** What the band shows before an overview exists
   (relates to B5's principle: explain why, do not show a permanent blank).
6. **A work-package cut** with file:line touch points, acceptance conditions,
   and explicit non-goals.

## Required output frontmatter (MANDATORY — the plan tooling derives state from it)

Your final written artifact MUST begin with exactly this YAML frontmatter:

```yaml
---
plan_artifact_id: plan_e0001372
intent_id: int_7c3e9a12
kind: deliberation
---
```

## Scope boundaries

- The agent-register artifacts are GOOD and must not be diluted — the human
  layer is strictly additive (dual-register, both durable).
- Do NOT redesign WP ingest (int_4f8b2d61, decided) or the promote path
  (int_9e5d0c47, in flight). Consume their interfaces.
- No new orchestration modes. Legacy HTML plan surface untouched.
