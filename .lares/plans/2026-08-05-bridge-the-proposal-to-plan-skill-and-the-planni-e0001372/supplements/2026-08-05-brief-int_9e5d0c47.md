# Deliberation brief — int_9e5d0c47: promote-path single writer (C1)

You are deliberating ONE marked intent of plan `plan_e0001372` ("Bridge the
proposal-to-plan skill and the planning surface"). Read these first:

1. The source proposal: `.lares/proposals/2026-08-05-planning-surface-skill-bridge.md`
2. The already-decided sibling deliberation (BINDING context — its design
   assumes the skill+reconciler path wins; see its final "Promote-path
   dependency" section for what changes if it does not):
   `.lares/plans/2026-08-05-bridge-the-proposal-to-plan-skill-and-the-planni-e0001372/deliberations/2026-08-05-int_4f8b2d61-wp-ingest-implementability-chain.md`
3. The code (validate, do not trust prose): `src/main/plans/promote-proposal.ts`,
   `src/renderer/components/plan/PromoteDialog.tsx`,
   `src/renderer/components/plan/PromoteToPlanPanel.tsx`,
   `src/main/database.ts` (`enrichAdoptedPlanRow`, promotion saga ~8003-8065,
   `promotion_requests` table), plus startup wiring and preload/IPC types.

## The question

Decide and specify the SINGLE promote story. A codex spot-check corrected the
proposal on a load-bearing point: **the saga is NOT wholly dead** — its IPC
service is wired during startup and its pending-request reconciler runs; only
the renderer dialog (`PromoteDialog`) is unmounted, while the mounted
`PromoteToPlanPanel` dispatches a skill-running supervisor. So C1 is a
migration decision, not a dead-code deletion.

Decide, with reasoning, between:
- **(a) Bless skill+reconciler** as the sole writer and retire the saga; or
- **(b) Mount the real dialog/saga** and make the skill call the same service.

The proposal leans (a) (disk-as-truth; a live saga reintroduces a second
writer), and the int_4f8b2d61 design assumes (a) — but you own this call.
Argue it on the evidence; if you choose (b), say precisely how the
int_4f8b2d61 reconciliation service stays the single package parser/writer.

Whichever you choose, deliver:

1. **Migration policy** for currently-persisted state: existing rows in
   `promotion_requests`, in-flight/pending requests at upgrade time, the
   boot-time reconciler's behavior, and any latches or claim scanning.
2. **A complete deletion/retention inventory** — not just `PromoteDialog.tsx`
   and `promote-proposal.ts`, but IPC handlers, preload bindings, shared types,
   startup wiring, the `promotion_requests` table (drop vs. retain-inert; note
   the workspace's readiness-gated-drop precedent), tests, and fixtures. Mark
   each item delete-now / retain-inert / keep.
3. **What replaces the saga's non-package value**, notably proposal-link
   enrichment (`plan_documents`) — B2's fallback reads
   `plan.json.source_proposal.rel_path`; say whether enrichment stays
   saga-owned, moves to the reconciler, or is dropped.
4. **The user-visible promote gesture** end state: exactly one path from a
   proposal card to a live plan folder, and what the mounted panel does.
5. **A work-package cut** with file:line touch points, acceptance conditions,
   and explicit non-goals.

Compatibility rule: a user upgrading with a pending promotion request must not
lose it silently, and no code path may end up with two writers of plan or
package rows.

## Required output frontmatter (MANDATORY — the plan tooling derives state from it)

Your final written artifact MUST begin with exactly this YAML frontmatter:

```yaml
---
plan_artifact_id: plan_e0001372
intent_id: int_9e5d0c47
kind: deliberation
---
```

## Scope boundaries

- No change to agent-register artifact formats (bundle contract, ARC ledger,
  sentinels, rung ladder). No new orchestration modes. Legacy HTML plan
  surface untouched.
- Do NOT redesign WP ingest or the overview band — those are int_4f8b2d61
  (decided) and int_7c3e9a12 (in flight). Consume their interfaces.
