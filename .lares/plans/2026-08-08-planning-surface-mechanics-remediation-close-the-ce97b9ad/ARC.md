# ARC — Planning surface mechanics remediation — close the gaps the audit found   (plan_sku: 2026-08-08-planning-surface-mechanics-remediation-close-the-ce97b9ad · plan_artifact_id: plan_ce97b9ad)
<!--ARC-META {"last_refreshed_at":1786237444200,"source_cutoffs":{"folder_mtime_ms":1786237430710.3389,"ledger_updated_at":null}} -->
## Decisions
- 2026-08-09 — Hardening scope verdict migrated from the proposal (see ## Hardening scope).
- 2026-08-08 — Hardening triage (scope): int_b7d41c2e (Cluster B, groupthink-serial), int_c2a90f13 (C2, research), int_d3f8266b (D3, research), int_d1d47a05 (D1+D4, groupthink-parallel); A/C1/D2 need no hardening. Second opinion (independent fresh-context agent) adopted on all four deltas vs the author's recommendation.
- 2026-08-08 — D3 resolved as RECORDER BUG (not concurrency semantics): openByAgent state-ownership race + identity-unsafe clearAgent + newest-50 startup reconciliation gap. Fix spec in the returned forensics; feeds D1 schema design.
- 2026-08-08 — C2 mechanism decided by research: provider hooks are not a boundary; target an OS-level restricted-token wrapper launcher (Codex-style synthetic SID + outbox ACE), hooks as defense-in-depth only.
- 2026-08-08 — D1+D4 settled by converged two-planner deliberation: SQLite = execution authority, git = portable artifacts; 4 new tables + one transition service as sole plan_work_packages.state writer (source-guard tested); plan.json IS git-tracked (identity/responsibility only, never execution state); population migration is ONE reviewed explicit-manifest commit preserving foreign ARC.md edits and the frozen specimen's content freeze.
- 2026-08-08 — Cluster B enforcement design settled by reviewer-approved deliberation: M1 ingest validation (PLAN-WORK-PACKAGES:v2 + grandfather quarantine) is the mechanical layer shipped by this cluster; M2 revert-refutation evidence is generated/recorded but completion-blocking only when D1's executor lands; seam truth and construct completeness stay human (S1/S2). F1/B4 are only PARTIALLY remediated until the completion refusal ships — do not advertise otherwise.
- 2026-08-09 — IMPLEMENTATION TRIGGERED by Edward ("launch codex workers and begin"). Wave 1 dispatched: WP-A1/A2/B1/D1 (disjoint file scopes). Held for sequencing: WP-C1 (shares plan-work-package-ingest.ts with B1), WP-ID1 (shares database.ts with D1), WP-C2A (research-write-guard.mjs foreign-dirty), WP-D7 (population migration commit needs a quiet index; save-card peers WP-U1/U2 mid-flight).
- 2026-08-08 — Packaged: 18 WPs in supplements/2026-08-08-work-packages.md (validated: strict JSON, 18/18 prose parity, dep-order clean). Baseline tag plan-baseline/2026-08-08-planning-surface-mechanics-remediation-close-the-ce97b9ad created (annotated, LOCAL ONLY — never push) at 91c2e227b6a495e07b2d2e013dc3f7bfe06a498d. ADVISORY: 79 uncommitted paths in the working tree at tag time — the tag captures committed HEAD only. Recovery: any file a WP deletes is one `git show <tag>:<path>` away; no copy-aside archiving.
## Work packages
- WP-A1 Scope and label file activity correctly — DONE 2026-08-09, commit 40781f2d (codex 0786e95f, option (a): idle events request current-session activity via pre-existing getFileActivities currentOnly param; explicit "Files touched in current session:" heading; 47-row/3-generation fixture; gated against tree — heading + wiring verified by read, 24 payload-builder + supervisor sibling suites green; repo-wide tsc blocked by concurrent D1/C1 in-flight edits, touched-area tsc clean)
- WP-A2 Make ARC staleness computed and visible — DONE 2026-08-09, commit d5dc4b5b (codex 4692bad3; scope widened justifiably to reader surfaces: planning-reader, plan-documents, PlanDocumentTabs stale badge, shared types. Gated against tree: seconds/ms unit detection + normalization verified; partial-stage of shared types.ts verified CLEAN — 0 foreign ResolvedIntentStamp lines in the commit, foreign hunk preserved in worktree. Calculator flagged 4 stale folders: e3a91c4f, e0001372, pigt5a83, 65e665d7; legacy-seconds 0e1425af normalized+fresh. 80 targeted main tests + 9 renderer green; 11 foreign save-card renderer failures noted, not ours. Temp dir .lares/tmp-wpa2-dist removed by supervisor.) CLUSTER A COMPLETE — B–D verification ordering constraint satisfied.
- WP-B1 PLAN-WORK-PACKAGES:v2 reachability schema in the ingest parser — DONE 2026-08-09, commit 350de9c7 (codex 800893d3, gated against tree: exported validator + reachability-invalid diagnostic + v2 gating + contentHash participation verified by read; 31 ingest tests + tsc clean; commit scoped to exactly its 2 files)
- WP-B2 Reachability persistence, grandfather snapshot, and quarantine — DONE 2026-08-09, commit 2d9b2ac2 (codex 275ac3d3; obligation+evidence tables, §4.6 cleared(), grandfather snapshot, legacy-unmigrated projection + dispatch/assignment/execution quarantine threaded into plan-implement.ts (small justified extension); foreign-symbol audit 0; 71 focused tests + committed-index build green. Recorded clause limitation: pre-B2 population identified via structured-v1 source rows since schema_version wasn't previously persisted. FLAG: worker observed 2 database.turn-witness-reads failures introduced by the save-card peer's landed WP-1 — foreign breakage, surface to supervisor dc4a15ae.)
- WP-B3 prove_reachability proof engine and verification target registry — DONE 2026-08-09, commit 7cfcdfda (codex b7f85803; gated against tree: 886 insertions across 5 files; partial-stages of shared ipc-handlers.ts + preload/index.ts verified CLEAN — exactly 3-line prove_reachability wiring hunks each, 0 foreign saveCard/adoptAllAsBaseline symbols in commit, foreign hunks preserved in worktree; registration-existence test proven through REAL registerIpcHandlers (prover suite 6/6, neighboring real-registration suite 13/13, touched-area tsc exit 0); detached scratch worktree per obligation, temp-index specimen from exact base OID, entry-link + production-construct refutations, node-test/vitest adapters, indeterminate classification for stale/protected/compile failures, transactional WP-B2 evidence persistence. Unperformed: full suite/build (foreign save-card tsc errors), legacy-plan-importer sibling failure pre-existing.) CLUSTER B COMPLETE (B1+B2+B3+B4 all landed).
- WP-B4 Managed scaffold deployment of the reachability skill and worker duties — DONE 2026-08-09, commit f9b017d5 (codex a822dbe5; gated against tree: skill deployed to all 4 lane roots, WORKER_CLAUDE_MD v11→v12 with cumulative previousHashes 1–11, codex AGENTS.md v4→v5 with 1–4, package.md v3→v4, work-packages.md contract v2→v3, derivePlanSku prohibition intact; 155 scaffold-migration + 27 worker-scaffold + 6 codex-parity tests green, frozen hashes independently re-derived. Unperformed: full test:supervisor timed out at 124s — targeted suites cover the change; prove_reachability n/a until B3.)
- WP-C1 Validate prose counts against machine blocks — DONE 2026-08-09, commit 9f109dc0 (codex 41b228d7, prose-mismatch diagnostic verified in tree; 33 ingest tests + touched-area tsc green). Live-instance fix applied on disk to plan_65e665d7 supplement, UNCOMMITTED BY DESIGN (rides WP-D7 migration): validator found FOUR divergences, not one — WP-1 five→seven, WP-3 six→eight, WP-4 four→five, WP-5 six→seven conditions. Foreign ARC.md untouched. Supervisor must record this fix in plan_65e665d7's ledger at D7 time.
- WP-C2A Outbox tier-1 hardening — DONE 2026-08-09, commit b56723cb (codex 46b3d292; gated: shell-write second line of defense in guard (deny exit 2, documented bypassable), codex writable_roots validated against codex 0.146.0 strict config, agy write grants seeded, researcher-settings v2→v3 / guard v5→v6 / codex config v6→v7 all with cumulative previousHashes (159 migration tests green); foreign block() comment hunk preserved verbatim + attributed in commit message; build:main green. Limitations documented in-code: shell parsing bypassable; WSL enforcement unverified; agy regexes no OS boundary.)
- WP-C2B Restricted-token outbox wrapper launcher (provider-neutral, Windows) — DONE 2026-08-09, commit c6736faf (codex 25319e1b; gated against tree: new src/main/sandbox/outbox-launcher.ts+test (490 lines) + 22-line researcher-only wiring hunk in supervisor/index.ts partial-staged CLEAN (0 foreign symbols; gated on roleLaneOf==='researcher', real runner.launch production path). EMPIRICAL Windows 11 Home evidence: unelevated restricted-token + synthetic-SID ACE succeeded; real child outbox write allowed, outside write DENIED; audit fail-closed on traversal errors; token composition matched Codex's official windows-sandbox-rs. Research claim "ACL stamping requires elevation" REFUTED empirically for user-owned outbox. Launcher 4/4 incl. real-token integration, supervisor siblings 54/54, tsc main exit 0. Unperformed: live researcher launch, network containment, non-Windows CI, WSL. Foreign staged work preserved.)
- WP-ID1 Portable identity enforced at ingestion — DONE 2026-08-09, commit 0c19579a (codex ac778a0c; gated against tree: 21 files, 391 insertions; database.ts partial-stage verified CLEAN — 0 save_intents/SaveIntent/ContinuationAttemptBinding symbols in commit; prop_/plan_/int_[0-9a-f]{8} enforced at ingestion seams with visible quarantine/rejection; fresh orchestration launches require valid active same-plan plan_id+planning_intent_id (legacy resumed rows may stay unbound, historical a1bacc4a unbound); guarded migration exact ID/path pairs via applied_migrations. BACKFILL MAPPING SUPERVISOR-REVIEWED AND APPROVED: all 7 pairs (e3a91c4f, e0001372, 0e1425af, 5b3ea7d1, 65e665d7, ce97b9ad, ac37e3ae) verified against proposal artifact_id/promoted_to + plan.json plan_artifact_id/source_proposal on disk; non-contract pigt5a83 excluded. 12 targeted suites green (169+ tests), build:main green on final tree. FLAGGED: 8 failing plan sibling files + groupthink-v2-prompts attributed to stale/retired surfaces (legacy-plan-importer pre-existing, promote-proposal.core et al. reference APIs deleted by plan_e0001372) — sweep at D8 acceptance.)
- WP-D1 Ledger schema: four new tables and guarded extensions — DONE 2026-08-09, commit 9e88b638 (codex 229f5177; all four tables + indexes verified in tree, lifecycle CHECK rebuild with schema-probe run-once guard, 40-hex OID validation, 345-line dedicated suite; build:main + 15/15 database suites green on committed index; partial-stage verified clean — remaining database.ts dirt is foreign save-card work (save_intents/SaveIntent), preserved. No §3 deviations claimed.)
- WP-D2 Single package-state transition service — DONE 2026-08-09, commit 7ae8dd5f (codex ffdc5dde; gated against tree: closed 9-command union, idempotency keys with conflict detection + corrupt-duplicate guard, one getDb().transaction per command, gate-failure→blocked with preserved failure row on passed retry, per-kind completion prerequisites (code/research/no-change), reachability-freshness consumption via getPlanWpReachabilityClearance; 7 focused + 7 D1 suite tests green per worker; commit = exactly its 2 new files, zero foreign-hunk risk. §4 writer-rerouting/source-guard/production-seam clauses deferred to D3/D5 as scoped.) M2 REVERT-REFUTATION IS NOW COMPLETION-BLOCKING — the executor exists; F1/B4 "partially remediated" caveat lifts once D3 routes writers through it.
- WP-D3 Route all existing package-state writers through the service — DONE 2026-08-09, commit 41243221 (codex 9de52d1f, worker crashed AFTER committing, summary lost — gated by supervisor from tree + chat log: 4 authorized files only, foreign-symbol audit 0 (worker's own prepared-index audit caught and rebuilt a finalizeSaveUnit-contaminated blob before committing); source-guard test present; remaining database.ts UPDATE plan_work_packages sites set only intent_id/metadata, never state; finalization done flip enters via plan-ipc codeCompletionDeclaration → transitionPlanPackage complete inside the finalization txn. Worker's last pre-crash report: staged-tree tsc clean in isolated scratch checkout, source-guard + package-ledger suites green. LOST WITH CRASH: its enumerated list of stale finalization/lifecycle fixture failures — sweep at D8.)
- WP-D4 Gate and deployment evidence ingestion — DONE 2026-08-09, commit cae8d732 (codex 86c2f1ae; gated against tree: 3 new files, 552 insertions, zero foreign-hunk risk; all ingestion routes through transitionPlanPackage (gates.ts:100, deployments.ts:84) — no direct state writes; absent gate rows read unknown-never-success (gates.ts:153), absent deployment rows unknown-not-not_required (deployments.ts:126); explicit not_deployed/not_required adapters, no git/push/reachability inference anywhere in deployments.ts; 5 ingestion + 7 sibling ledger tests green, build:main + tsc --noEmit passed. Unwired IPC per non-goals.)
- WP-D5 Continuation handoff results and checkpoint recorder fixes — DONE 2026-08-09, commit c7c46d75 (codex 1a086b22 r3; gated against tree: 12 files, 405 insertions, foreign-symbol grep of database.ts + supervisor/index.ts hunks = 0 (SaveIntent family excluded, blob clean per worker's isolated-worktree validation); acceptance verified in tree: brick_saved seams in continuation-watcher.ts:592/620/667 with failed/timed_out outcomes, successor_started at index.ts:7367/7390, successor_oriented at index.ts:2688-2730, all keyed handoff_attempt_id into new continuation_handoff_result_events table (database.ts:336, no checkpoint-turn status writes); reconcileOpenTurns → listOpenTurnRecords (turn-coordinator.ts:426, database.ts:9344, unbounded); rawPath.trim() ingress canonicalization in witness-recorder.ts; pending-AFTER-capture race test at turn-coordinator.test.ts:347. Targeted 128 + isolated-worktree 102 tests green, build:main passed. Worker flagged pre-existing sibling failure database.planWorkPackages.test expecting direct ready→executing — stale vs WP-D3 ledger routing, ADD TO D8 SWEEP LIST. r1 73e1e4ed hang / r2 01fe5cd3 read-only apply_patch — both zero changes)
- WP-D7 Plan-folder version-control policy — DONE 2026-08-09, commits b8c77b0f (policy/checker/tests/package.json — exactly the 4 declared files) + 7a9bb278 (migration: 56 files, ALL under .lares/plans/, 8708 insertions ZERO deletions, forbidden-path grep 0 — no 5b3ea7d1/65e665d7 ARC.md, no C1-rider supplement, no src/). (codex d3000ee3 r1; gated against tree: foreign ARC.md edits + C1 rider verified still-unstaged in worktree post-commit; 5b3ea7d1 specimen worktree-vs-HEAD diff = ARC.md only (the preserved foreign edit) → staged content byte-identical; .gitignore hunks anchored lock/temp-only in both state-dir roots (lines 83-86), durable paths not ignored; checker prepared-index + post-commit runs passed 81/0/0/0, 4/4 fixture tests. NOTE: foreign save-card commit bb823b15 (WP-4 candidates-by-intent) landed mid-window between D7's two commits — peer workstream activity, b8c77b0f remains ancestor of 7a9bb278, migration unaffected. CROSS-PLAN RECORD: plan_65e665d7's supplement fix (C1 rider) remains uncommitted BY DESIGN — recorded here because that plan is owned by supervisor c30eb66f; not seizing assignment for a note. Surface to Edward.)
- C2A-FIX (post-plan follow-up, Edward-approved 2026-08-09) — DONE, commit c1ba9416 (codex e6a0e6ce r1; gated against tree: 2 files only, foreign-symbol grep 0, prepared-index blob audit per windows-partial-staging-blob; normalizeCodexArgs now platform-seamed (default process.platform), win32 --full-auto → --dangerously-bypass-approvals-and-sandbox with why-comment, non-Windows workspace-write unchanged, buildCodexResumeArgs threads platform; 15/15 resume suite + build:main green; no other codex workspace-write hardcodes in src/main; CLI probe from trusted cwd confirmed bypass flags accepted (danger-full-access). REMAINING to close the sandbox-hang loop: rebuild + app restart, then one stock-launched codex worker must provably write the repo.)
- WP-D8 Ledger acceptance + stale-sibling sweep — DONE 2026-08-09, commit 375690b9 (codex 7f9c3ce7 r1; gated against tree: exactly 11 test files, 514-line acceptance suite + 10 fixture repairs, foreign-symbol grep 0 (incl. resolveActivityMerge); acceptance verified in tree: 7a synthetic a1bacc4a shape (acceptance.test.ts:209), reviewed provenance header (line 3), not_deployed rendering, 13-commit chain full OIDs resolved via rev-parse (e52ad5fb/b4617499 spot-verified as commits); sweep disposition: 10 fixed, promote-proposal.core/enrich + groupthink-v2-prompts + legacy-plan-importer already deleted by earlier plans (no D8 deletion), foreign save-card surfaces excluded and preserved — peer's post-commit reachability hunk verified still-unstaged. Worker verification: acceptance 3/3, 60-file compiled plan/database sweep 0 failures, build:main + tsc noEmit passed via scratch pattern (peer's preload breakage bypassed, not touched), plan-versioning checker 81/0. No Electron restart (F9), no push.) **PLAN COMPLETE: 18/18 WPs DONE + gated.**
- WP-D6 DB-only plan projection read surface — DONE 2026-08-09, commit fbf659e2 (codex 957a2961 r4; gated against tree: 5 files, 768 insertions, database.ts NOT touched (D5's ownership honored), foreign-symbol grep of plan-ipc.ts + preload hunks = 0; acceptance verified in tree: renderPlanFromLedger (plan-ledger-projection.ts:243) projects identity/source-proposal/intent/revision/dispatch/gates/commitChain/deployment/stateHistory from SQLite only; fs-throws DB-only proof at plan-ledger-projection.test.ts:175; bindingState derived at read time via deriveBindingState joins (projection.ts:61), no stored marker; IPC plan:ledgerProjection through real registerIpcHandlers + preload api.plans.ledgerProjection, both revert-refuted by worker. build:main + prepared-tree tsc passed, 4/4 registration suite green. Worker flagged 6 pre-existing red plan sibling suites (mission-board, package-dispatch, plan-ipc.finalize, plan-lifecycle, plan-work-package-ingest, proposal-to-implement.e2e) — stale vs landed D-wave ledger changes, FOLD INTO D8 SWEEP LIST. r1 9040d818 + r2 47209060 hang / r3 58b3d51c read-only apply_patch — all zero changes)
- INCIDENT 2026-08-09 (final diagnosis): TWO C2A/C2B regressions armed by the 22:13 post-crash restart. (1) C2A commit b56723cb remapped the codex worker launch flag `--full-auto` from `--dangerously-bypass-approvals-and-sandbox` to `--sandbox workspace-write --ask-for-approval never` — live in dist since the restart. (2) A C2 validation worker left `[windows] sandbox = "unelevated"` in GLOBAL ~/.codex/config.toml (18:50, experiment residue; supervisor removed 2026-08-09). On Windows codex 0.146.0: workspace-write + gate = PowerShell hangs at login-shell startup; workspace-write without gate = apply_patch read-only outside cwd. Either way workers are bricked — 5 launches lost (zero changes each). MITIGATIONS: global gate removed; deployed worker config restored to exact v7; workers launched with explicit command override `codex --dangerously-bypass-approvals-and-sandbox --dangerously-bypass-hook-trust` until the app is rebuilt+restarted. OWED (C2A follow-up): Windows-aware sandbox mapping in supervisor/index.ts (or revert to bypass) + end-to-end worker write probe before trusting any Windows sandbox mode. Memory capsule mb-2026-08-09-codex-sandbox-hang-hotfix holds the recovery recipe.
- WP-D7 Plan-folder version-control policy: ignore rules, checker, migration commit — ready — unassigned
- WP-D8 Ledger acceptance: synthetic gate and historical DB-only comparison — blocked (D5, D6) — unassigned
## Deliberations
- int_d3f8266b (d3-continuation-vs-checkpoint-semantics) — returned + folded-in 2026-08-08. Output: research/2026-08-08-d3-continuation-turn-forensics.md (codex worker eaaa41cf, read-only DB+code forensics). ran: unavailable (pre-ledger). Integration: resolved the semantics-vs-bug fork as recorder bug with file:line trace and correct-fix spec.
- int_c2a90f13 (c2-provider-neutral-outbox-sandbox) — returned + folded-in 2026-08-08. Output: research/2026-08-08-c2-provider-sandbox-capabilities.md (researcher 3b9930f7 via untrusted inbox, copied in by supervisor after review; unverified claims flagged in-document). ran: unavailable (pre-ledger). Integration: layered Tier1/2/3 containment design, Tier-2 restricted-token wrapper recommended.
- int_b7d41c2e (cluster-b-production-reachability) — returned + folded-in 2026-08-08. Output: deliberations/2026-08-08-int_b7d41c2e-cluster-b-enforcement.md (groupthink-serial run 73a2b4f9, claude lead / codex reviewer, reviewer-approved). ran: unavailable (pre-ledger; run id is self/system-declared cross-check). Integration: five-layer M1/M2/S1-S3 enforcement split; M2 non-blocking until the D1 completion executor lands (recorded limitation).
- int_d1d47a05 (d1-d4-ledger-schema-and-plan-folder-versioning) — returned + folded-in 2026-08-08. Output: deliberations/2026-08-08-int_d1d47a05-ledger-and-versioning.md (groupthink-parallel run 9a5148f5, claude + codex planners, converged). ran: unavailable (pre-ledger). Integration: 4 new tables + single transition service (sole state writer); D4 = track all durable files incl. plan.json; DB-only acceptance vs the 13-commit chain.
## Who did what
- Supervisor 3390b90a ("Planning supervisor -- Updating the Planning Surface Mechanics"): scope+mark, promote (scaffold via plan-manifest.mjs), responsibility event rev_ed4f2585337423c3, dispatch of all four hardening runs, integration of D3 + C2 outputs.
- eaaa41cf (codex worker): D3 forensics, read-only, delivered 2026-08-08.
- 3b9930f7 (claude researcher): C2 web research, delivered to inbox 2026-08-08.
