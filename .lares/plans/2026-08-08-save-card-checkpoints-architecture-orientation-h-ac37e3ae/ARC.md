# ARC — Save Card & checkpoints — architecture orientation, honest assessment, and directions   (plan_sku: 2026-08-08-save-card-checkpoints-architecture-orientation-h-ac37e3ae · plan_artifact_id: plan_ac37e3ae)
<!--ARC-META {"last_refreshed_at":1786237267308,"source_cutoffs":{"folder_mtime_ms":1786236908729.6262,"ledger_updated_at":null}} -->
## Decisions
- 2026-08-09 — Hardening scope verdict: one design deliberation (int_7d41c9a2: D5+D1+Q2 intent/lifecycle/concurrency architecture); all other parts package directly; no further research
- 2026-08-09 — Baseline tag `plan-baseline/2026-08-08-save-card-checkpoints-architecture-orientation-h-ac37e3ae` created (local annotated, NEVER push) at commit 91c2e227b6a495e07b2d2e013dc3f7bfe06a498d. Recovery framing: anything a WP later deletes is one `git show <tag>:<path>` away — no copy-aside archiving. Advisory: 79 uncommitted worktree entries at tag time are NOT captured by the tag (other workstreams' dirt; non-blocking).
- 2026-08-09 — WP-1 scope ruling: acceptance conditions (dispatch reuse, fork/revive carry) proved unreachable from the original Files list — `dispatchPlanPackage` had no production caller; fork/revive carry needs `src/main/supervisor/index.ts`. Supervisor minted WP-1b (prose addendum in the supplement; machine block deliberately unamended). WP-1 foundation committed flag-gated; WP-2/WP-5 remain gated on WP-1b landing, since WP-1 alone is not production-reachable.
- 2026-08-09 — Foreign tree breakage noted during WP-1: uncommitted ~210-line scaffold-constant insertion in `src/shared/constants.ts` (PROVE_PRODUCTION_ENTRY_POINT_SKILL graduation, unescaped backticks → 31 tsc errors both processes). Belongs to the peer planning-surface-mechanics workstream (no witnessed checkpoint row; live in-progress turn). Not fixed by this plan's workers; gates exclude these errors until the author lands it.
## Work packages
Supplement: [supplements/2026-08-09-work-packages.md](supplements/2026-08-09-work-packages.md) (kind: work-packages, PLAN-WORK-PACKAGES:v1, 11 packages). Prose Files lists are the scope authority.
- WP-U1 Save Card quick unblocks: readable subjects, path acks, bulk acknowledgement — LANDED+GATED 2026-08-09 — commit 679cc28d (codex e5bc097c, reaped). Gate: tree-verified subject/trailer/U+2026 logic, displayPath + acknowledge-all in committed diff, old "Save component:" gone from production sources, touched files tsc-clean (renderer 112 pre-existing errors elsewhere), 4 files exactly. Deviation accepted: SaveCard.tsx/shared contract untouched (acknowledge-all reuses per-file atoms). Manual Save-pane GUI check deferred to Edward's next live session.
- WP-U2 Batch the git work: single temp index, bounded parallelism, incremental sweep refresh — LANDED+GATED 2026-08-09 — commit fc5f8494 (codex 5353b53f, reaped). Gate: tree-verified single temp index (read-tree + --pathspec-from-file + one ls-files --stage -z), bounded revalidation pool (8), per-package full refresh removed (seam retained), parity + timing evidence (100-file preview 120ms/5 git calls, commit 271ms), 69/69 touched-subsystem tests, main tsc 0 re-run by supervisor. 6 files exactly.
- WP-1 Intent identity and immutable dispatch join — FOUNDATION LANDED 2026-08-09 — commit d48d9af1 (codex 189c572c, reaped). Flag-gated (intentPackaging / LARES_INTENT_PACKAGING=1); migration via initContextOptimizerSchema; suites green pre-foreign-breakage; wire-schema grep clean (no injectable intentId). NOT yet production-reachable — acceptance completes via WP-1b.
- WP-1b Production wiring for intent identity (supervisor addendum) — LANDED+GATED 2026-08-09 — commit 8848d87f (codex c5275dd3, reaped at 100% context). Gate: production chain verified in committed bytes (main index → registerPlanIpc(supervisor) → registerPlanPackageDispatchIpc → ipc.handle('plan:dispatchPackage') → preload dispatchPackage); production-obtained registration test present; focused suites 9/9, 22/22, 21/21, 23/23, fork/revive 3+37, continuation 75/75; wire-schema grep clean; main tsc 0 re-run by supervisor. WP-1 acceptance now COMPLETE. Finding: `Implement` previously had NO production package-brief route at all — WP-1b's plan:dispatchPackage bridge is new surface. 8 plan + 1 supervisor sibling-suite failures attributed (not proven) to live concurrent peer churn (WP-B2/WP-B4/WP-D1 landing + live database.ts edits); all WP-1b-scoped suites green.
- WP-2 Intent inventory and Save Card projection — LANDED+GATED 2026-08-09 — commit 9df5a956 (codex e8596139, reaped). Gate: 14 files, no constants.ts; production wiring in committed bytes (registerSaveCardIntentIpc + preload adoptAllAsBaseline + intentUnits in route); parity fixtures bundles-vs-intents; all 5 SaveCard-area renderer tsc errors blamed to pre-plan commits (total unchanged 112); suites green.
- WP-3 Concurrency classification and the cross-intent resolution picker — FOUNDATION LANDED+GATED 2026-08-09 — commit 783022f6 (codex 00c45fe4, reaped at 91%). Gate: 13 files, no constants.ts; database.ts committed via prepared index (foreign proposal-backfill hunks were landed by peer WP-ID1 0c19579a first — nothing lost); main tsc 0 re-run by supervisor. Supervisor ruling: registered-IPC cross-intent reachability test committed as skip-with-pointer ("WP-4 atom projection") because candidate-service selectedTopologyEvidence filters atoms to kind==='overlap' — picker reachability transfers to WP-4 (precedent: WP-1→WP-1b transfer). WP-4 MUST: (1) unskip+pass that test, (2) remove the overlap/acknowledgeTopologyDigest compat fields WP-3 retained, (3) wire picker persistence through Save Card IPC/preload.
- WP-4 Candidate and commit contract v2 with the intent ledger — dispatched 2026-08-09 — codex worker e9d2b4a4 (fresh session). Carries three transferred gates from WP-3 (unskip reachability test; remove overlap/acknowledgeTopologyDigest compat fields in-scope; picker persistence through production IPC/preload). Worker correctly stopped on gate C scope gap; supervisor minted "WP-4 addendum - Picker-persistence production wiring" in the supplement (preload/index.ts, shared/types.ts API contract, SaveCard.tsx callback wiring, CandidatePreview.tsx seam-only; WP-1b precedent, machine block unamended) and resumed the worker. Second stop same day: production v2 mint path unreachable — mint route lives in preview-routes.ts (v1 selections only) registered via ipc-handlers.ts; addendum Extension granted preview-routes.ts (v2 mint forwarding + gate-B removal of acknowledgeTopologyDigest on that path), ipc-handlers.ts (mint-adapter registration only), shared/types.ts (v2 mint transport contract); worker resumed again. CRASH 2026-08-09 ~04:48–05:35: implementation turn killed twice mid-flight (codex usage-limit restarts, restartCount 2); NO commit; partial work verified ON DISK uncommitted (~569 lines: candidate-service +215, database +198, finalization +111, shared/commit-candidates +73 — WP-4 signature tokens confirmed, not peer's); preview-routes/ipc-handlers/preload/types/coordinator/SaveCard/gate-A-unskip NOT started. Peer committed 41243221 (WP-D3 ledger) during the outage. Worker resumed 05:5x with re-derive-from-tree instructions (assume half-applied state, tsc early, recheck shared files for new foreign hunks). LANDED+GATED 2026-08-09 — commits bb823b15 (26 files, "package Save Card candidates by intent") + 573359a5 ("wire intent finalization production path" — second commit authorized because peer 7a9bb278 landed between; finalizeSaveUnit had no production caller, caught by worker's own reachability audit). Third stop granted as addendum Extension 2 (three v2 refusal-label entries in CandidatePreview's exhaustive map). Gate: all files in brief+addenda scope, no constants.ts; gate A test live at preview.test.ts:395 (supervisor-verified); gate B acknowledgeTopologyDigest=0 in scoped files; gate C production chain asserted (registerIpcHandlers → preload resolveAttribution → SaveCard.onCrossIntentResolution); main tsc 0 re-run by supervisor; renderer back to 112 pre-existing; ~130 focused tests green per worker incl. post-CAS ledger-write. Foreign facts reported not fixed: candidate-submit.ts test assertion, 2 WP-D3 finalization failures. Worker reaped at 100% context, 431 turns. Worker-reported WP-7 remnants outside WP-4 scope: overlap handling in component-assembler.ts; acknowledgeTopologyDigest in preview-routes.ts + save-sweep-service.ts — fold into WP-7 brief.
- WP-5 Planning-activity worktree provisioning and dispatch routing — LANDED+GATED 2026-08-09 — commit a3afa2d5 (codex 3f94af1c, reaped). Gate: 15 files, zero excluded-file hits (no constants.ts, no WP-3 files); production wiring in committed bytes (engine-bootstrap:424 reconciler, plan-implement:388 provisioning, refs/lares/activities service); crash fixtures steps 1–9; real `git worktree add` integration test; main tsc 0 re-run by supervisor. Deviations accepted → carried as WP-6 gate items: (1) atomic activity-ref advance primitive exists+tested but production Save call site (CommitCoordinator) not yet invoking it; (2) per-activity repository not yet rendered as separate Save Card card; (3) auto attach/reselection of dispatch agents to activity cwd deferred (refusal path works).
- WP-6 Merge-back, conflict UI, cleanup, and recovery — dispatched 2026-08-09 — codex worker 003bcc36 (fresh session). Brief carries WP-5's three gate items (production-wire atomic activity-ref advance via promotion path — stop-and-report if commit-coordinator.ts needed; per-activity card in save-card-routes projection; auto-attach stays deferred/refusal-only) + known foreign facts (candidate-submit.ts assertion, 2 WP-D3 finalization failures) + conditional second-commit rule learned from WP-4. First stop (correct, zero edits): three seams unreachable — commit-coordinator.ts Save call site, reconciler §5.4 rows, renderer/IPC production chain for PlanMergeBack. Supervisor minted "WP-6 addendum - Production seams for merge-back" granting the FULL chain up front (coordinator call-site, reconciler, SaveCard mount, shared contract + preload + registration + bootstrap-if-needed); worker resumed. Second blocker ENVIRONMENTAL: worker 003bcc36 was launched by the dashboard with a NEW default codex command (`--sandbox workspace-write --ask-for-approval never` instead of `--dangerously-bypass-approvals-and-sandbox`) — repo root outside the writable sandbox (codex cwd is .lares/workers/codex), apply_patch rejected read-only. Stopped 003bcc36; relaunched as 362f0cf2 (r2) with explicit full-bypass command override matching all six prior WP workers. ⚠ FLAG FOR EDWARD: the dashboard's default codex launch command changed mid-session (likely peer workstream) — future codex workers need the explicit command override or the new default needs a wider sandbox root. LANDED+GATED 2026-08-09 — commit 892e57bc (21 files, 1460+/56-). Gate: all files in base+addendum scope, no constants.ts; accepted deviation: merge-service singleton bootstrap in src/main/index.ts (+9) instead of engine-bootstrap.ts (same minimal seam); production chain verified (registerActivityMergeIpc at ipc-handlers.ts:178/320, preload resolveActivityMerge, SaveCard mounts PlanMergeBack, coordinator → advancePlanningActivityHead + eager promote); §9 scenarios 4–6+10 5/5 real-Git; recovery matrix fixtures per §5.4 row; ~250 tests green; only known foreign WP-D3 finalization failures remain (candidate-submit failure did NOT reproduce); main tsc 0 re-run by supervisor; renderer 112 unchanged. All three WP-5 carried items closed (1 and 2 done; 3 stays deferred by design). Worker 362f0cf2 reaped at 100%/168 turns.
- WP-D3 Provenance durability: Assisted-by policy and checkpoint-ref pinning — dispatched 2026-08-09 — codex worker 351c6ee2 (fresh session, explicit full-bypass command per sandbox flag above). Commit subject convention "WP-D3(plan): ..." to disambiguate from the peer's unrelated WP-D3 (41243221). Sequenced BEFORE WP-D6 (both touch commit-coordinator.ts). First stop (correct, zero edits): trailer derivation production seam relocated by WP-4 to preview-routes productionSeams (defaultDeriveTrailers bypassed); CandidateTokenSnapshot lacks provider/model provenance; retention lacks promoted-commit→checkpoint-ref production input. Supervisor minted "WP-D3 addendum" granting preview-routes deriveTrailers impl, snapshot provenance freeze (candidate-service + shared), index.ts injection seam if needed, merge-service/database linkage; worker resumed. LANDED+GATED 2026-08-09 — commit 2746573f "WP-D3(plan): preserve witnessed provenance and promoted refs" (15 files, 446+/22-). Gate: all files in base+addendum scope, zero renderer/preload/constants hits; opt-out via `git config lares.assistedBy` verified in production seam bytes (preview-routes.ts:752); witnessed-only trailers, UUID-free shareable shapes, Lares-Checkpoint-Ref-Local labeling, promoted-ref pins survive real prune pass (retention 19/19); coordinator 19/19, merge 5/5, preview 15/15, E2E green; only the 2 known foreign finalization failures remain; main tsc 0 re-run by supervisor; renderer 112 unchanged. Accepted deviation: index.ts unneeded (defaults installed at authorized construction seams). Worker 351c6ee2 reaped.
- WP-D6 Opt-in candidate-tree validation and repo-policy signing — LANDED+GATED 2026-08-09 — commit 6800ca25 (codex 718985ce, reaped). Gate: 5 files exactly in scope (candidate-validation.ts/.test NEW, coordinator pre-CAS hook, shared types, production-seam test), no constants.ts; flag gate at coordinator seam (LARES_INTENT_PACKAGING off → forced-disabled policy inline) plus repo-local opt-in (`lares.candidateValidation.enabled` git config) — off-by-default at both layers; refusal returns aborted-error BEFORE commit-object creation and HEAD CAS, proven by production-seam test constructing the real coordinator with production defaults (incomplete candidate refused + HEAD unchanged; complete tree lands); worker ran feature-off coordinator suite 19/19 + fixtures 3/3; supervisor reran tsc main 0 and renderer 112 (unchanged); all 5 committed blobs match worktree; peer's 2 staged save-card-ipc blobs preserved.
- WP-7 Cutover, legacy removal, and end-to-end verification — dispatched 2026-08-09 — codex worker 99c79f49 (fresh session, explicit full-bypass command; all deps WP-U2/WP-6/WP-D3/WP-D6 landed). Briefed: supplement ## WP-7 + design §8/§9; known remnants (component-assembler generic overlap, acknowledgeTopologyDigest in preview-routes + save-sweep-service); ten §9 scenarios; full suites green before flag removal, flag removal LAST; 2 foreign finalization-service failures report-not-fix; peer's 2 staged save-card-ipc blobs to preserve; complete-seam-map stop rule. Stopped correctly on first pass (zero edits) reporting the renderer chain gap; supervisor minted "WP-7 addendum - Renderer chain completion" (SaveBundle.tsx retire/convert, save-card-expiry.ts DTO migration, sibling tests) and resumed the worker 2026-08-09. Second correct stop (zero edits): full src/ audit found one remaining bundle consumer — plan-review-projection.ts (+test); supervisor minted "WP-7 addendum 2 - Plan-review projection migration" and resumed again. LANDED+GATED 2026-08-09 — commit b37b1c37 (worker reaped). Gate: single commit atop 6800ca25, 76 files (+1,138/−4,298); constants.ts untouched; peer's 2 staged save-card-ipc blobs verified byte-identical in the index after landing; supervisor-rerun dead-symbol grep of ALL removed names (WorkBundle, SaveCardBundle, requiresOverlapAck, acknowledgeTopologyDigest, component-subset-not-allowed, overlap-ack, LARES_INTENT_PACKAGING, intentPackaging) = 0 hits in src/ + scripts/; supervisor-rerun tsc main 0, renderer 106 (−6 from 112 baseline, the drop consistent with deleted legacy renderer files; none in WP-7 files); all committed blobs match worktree; runner deregistered work-bundle.test and registered intent-architecture-telemetry.test. Worker-run: all suites green (commit-candidate 34/35 files — sole failure the known-foreign finalization-service 2; checkpoint 42/42; planning 42/42; renderer save 79 tests; production-IPC registration/preload/mint/finalize green), all ten §9 scenarios passed incl. crash-recovery idempotency, main + renderer production builds green AFTER flag removal (flag removed last, as briefed). NOTE: worker rebuilt renderer/main dist — the running app picks up changes on next restart. PLAN IMPLEMENTATION COMPLETE: every WP (WP-U1…WP-7 + supervisor addenda) landed+gated; intentPackaging flag retired — intent-first save is now the unconditional code path.
## Deliberations
- int_7d41c9a2 (D5 intent-first packaging + D1 graduated concurrency + Q2 activity worktrees) — rung: **folded-in**. GroupThink parallel (self-declared run 5432c132, lead claude / reviewer codex; `ran` unavailable pre-ledger — run id is a cross-check only). Output: [deliberations/2026-08-09-intent-architecture-design.md](deliberations/2026-08-09-intent-architecture-design.md), folded into plan.md §5 with PLAN-INTEGRATION 2026-08-09. Both planners converged; 13 disagreements resolved (task-level intent identity finer than plan item; per-path blob OIDs not commit OIDs; supersession human-only; app-owned detached worktrees on refs/lares/activities; companion lifecycle table; eager per-task promotion; proof-gated cleanup). Yielded the staged WP1–WP7 decomposition consumed by `package`.
- D2 (hunk-level attribution) deliberately NOT marked this cycle — sequenced after D5's contracts exist; a fresh intent will be minted then (§R1).
## Who did what
- Scope second opinion: codex worker a5fdcfb9 (read-only triage, 2026-08-09, reaped).
- Deliberation: GroupThink run 5432c132 members (dashboard-launched, muted).
- Scope/promote/integrate/package: responsible supervisor dc4a15ae ("Planning supervisor — Refining Save Card in App Experience").
- 2026-08-09 — Edward triggered implementation (codex workers). Dispatch wave 1: WP-U1 → codex e5bc097c, WP-U2 → codex 5353b53f (parallel, disjoint files, fresh sessions). Gate on tree evidence, not summaries.
